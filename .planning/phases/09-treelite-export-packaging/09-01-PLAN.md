---
phase: 09-treelite-export-packaging
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - python/tests/test_export.py
  - python/tests/conftest.py
  - python/sylva/export.py
autonomous: false
requirements:
  - EXP-01
  - EXP-02

must_haves:
  truths:
    - "treelite 4.7.0 and tl2cgen 1.0.0 are installed and importable in the dev environment"
    - "It is confirmed whether `import_from_json` exists in treelite 4.7.0; the result is recorded in a committed decision comment at the top of export.py"
    - "For a tiny 1-tree binary classifier ForestIR, `export_to_treelite()` returns a treelite.Model without error and `treelite.gtil.predict()` produces an output array of the correct shape"
    - "The postprocessor choice (identity vs sigmoid vs softmax) for clf and reg is empirically verified against Sylva's own predict_cpu output; the decision is recorded in export.py"
    - "Failing round-trip parity tests exist for clf and reg (may be RED at this wave — the spike's job is to confirm postprocessor semantics, not achieve final GREEN)"
    - "conftest.py exports session-scoped trained_clf_ir_small and trained_reg_ir_small fixtures (50 trees, max_depth=6)"
  artifacts:
    - path: "python/sylva/export.py"
      provides: "Skeleton export_to_treelite() with spike decision record comment"
      contains: "export_to_treelite"
    - path: "python/tests/test_export.py"
      provides: "Round-trip parity tests (gtil clf+reg) — at minimum in a callable/runnable state"
      contains: "test_gtil_round_trip"
    - path: "python/tests/conftest.py"
      provides: "Session-scoped small-forest fixtures"
      contains: "trained_clf_ir_small"
  key_links:
    - from: "python/tests/test_export.py"
      to: "python/sylva/export.py"
      via: "from sylva.export import export_to_treelite"
      pattern: "export_to_treelite"
    - from: "python/sylva/export.py"
      to: "crates/sylva-core/src/ir.rs"
      via: "json.loads(ir_json) hydrates ForestIR fields; tree_offsets[t] used for local_id reindex"
      pattern: "tree_offsets"
---

<objective>
Spike Wave: resolve the two blocking unknowns before committing to the full export implementation.

Unknown 1 (D-01, per orchestrator locked decision): Does `treelite.import_from_json()` still exist as a 3.x shim in treelite 4.7.0? Run `python -c "import treelite; print(hasattr(treelite, 'import_from_json'))"` — if True, the 3.x JSON approach is available as an alternative; if False, `ModelBuilder` is the only path. Record the result in a comment at the top of `export.py`. Per locked decision D-01, the ModelBuilder path is the LOCKED implementation regardless; this spike confirms whether a JSON alternative also exists.

Unknown 2 (D-03 classifier postprocessor — locked as `identity`, but must be verified empirically): For a tiny ForestIR whose leaves already store class probabilities summing to 1.0, does `postprocessor="identity"` with `average_tree_output=True` produce output matching Sylva's own `predict_cpu`? The spike must verify this empirically against the `tiny_clf_ir` test fixture and commit the result.

This plan also creates the test scaffold (conftest fixtures, test_export.py stubs) that Wave 1 fills in, ensuring the test infrastructure exists before the full export function is implemented.

Purpose: Eliminate the two unknowns that would cause Wave 1 to be built on assumptions. A committed decision record means Wave 1 can be implemented confidently.

Output: `python/sylva/export.py` (spike skeleton + decision record), `python/tests/test_export.py` (parity test stubs, at minimum one gtil round-trip runnable), `python/tests/conftest.py` (updated with export fixtures), committed spike result.
</objective>

## Assumptions

| Decision | Implementation | Alternative (if spike proves otherwise) |
|----------|----------------|----------------------------------------|
| D-01: ModelBuilder path (LOCKED) | `treelite.model_builder.ModelBuilder` API | If `import_from_json` exists in 4.7, note it as available but do not use it — locked per orchestrator |
| D-03: `postprocessor="identity"` (LOCKED) | Spike verifies empirically; if wrong, the fix is one parameter change in export.py | If `identity` fails parity, try `kMultiClf` with 2-class vector leaves (binary case) or other postprocessors and record which works |
| D-04: global→tree-local reindex (LOCKED) | `local_id = global_id - tree_offsets[t]` on every child key | No alternative — this is structural (ForestIR is global-SoA; ModelBuilder is tree-scoped) |
| D-05: `leaf_proba` already sums to 1.0 | Proven by `ir.rs test classifier_leaf_proba_sums_to_one` | No alternative needed — contract holds |
| D-02: export Python module receives IR as JSON string | `json.loads(ir_json)` consistent with pyseam contract (OQ-5) | No direct PyO3 binding needed — pure-Python transform |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-treelite-export-packaging/09-RESEARCH.md
@.planning/phases/09-treelite-export-packaging/09-PATTERNS.md
@.planning/phases/09-treelite-export-packaging/09-VALIDATION.md
@.claude/CLAUDE.md
@crates/sylva-core/src/ir.rs
@crates/sylva-core/src/pyseam.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install treelite/tl2cgen and run the API spike (EXP-01 prerequisite)</name>
  <files>python/sylva/export.py</files>
  <read_first>
    - .planning/phases/09-treelite-export-packaging/09-RESEARCH.md (Critical Finding §import_from_json; §Open Questions 1, 2, 3; §Pattern 1 ModelBuilder walk; §Package Legitimacy Audit — treelite and tl2cgen both cleared as SUS false positives)
    - .planning/phases/09-treelite-export-packaging/09-PATTERNS.md (Pattern: "python/sylva/export.py" section, imports block, input validation pattern, error handling pattern)
    - crates/sylva-core/src/ir.rs (ForestIR struct definition lines 22-65; tree_offsets field line 57; leaf_proba layout line 50-52; leaf_offset line 53; classifier_leaf_proba_sums_to_one test lines 208-218)
    - crates/sylva-core/src/pyseam.rs (py_fit_cpu lines 228-270 for the serde_json::to_string pattern; sylva_error_to_pyerr lines 46-53 for the error-mapping pattern)
  </read_first>
  <action>
    Install treelite and tl2cgen into the dev environment: `pip install treelite==4.7.0 tl2cgen==1.0.0`.

    Run the API probe (can be a scratch Python one-liner or small script, do not commit the probe script itself):
    - `python -c "import treelite; print('import_from_json exists:', hasattr(treelite, 'import_from_json'))"` — record the boolean result.
    - `python -c "from treelite.model_builder import ModelBuilder, Metadata, TreeAnnotation, PostProcessorFunc; print('ModelBuilder importable')"` — confirm the 4.x import path works.

    Then create `python/sylva/export.py` as a proper module (not a scratch script). The file must:
    - Begin with a module-level docstring containing the spike result comment (per D-01): whether `import_from_json` exists in the installed treelite 4.7.0, and the confirmed locked decision to use `ModelBuilder`.
    - Import `json`, `numpy as np`, `treelite`, and from `treelite.model_builder` import `Metadata`, `ModelBuilder`, `PostProcessorFunc`, `TreeAnnotation`.
    - Define a helper `_validate_ir_dict(ir: dict) -> None` that checks the required top-level keys (`n_trees`, `n_features`, `tree_offsets`, `feature_id`, `threshold`, `left_child`, `right_child`, `default_child`, `is_leaf`, `leaf_offset`, `leaf_proba`, `leaf_value`, `task`) are present and that `len(tree_offsets) == n_trees + 1`; raises `ValueError` with a descriptive message if not. This mirrors the `validate_structure()` pattern from `ir.rs` at the Python boundary.
    - Define the main function `export_to_treelite(ir_json: str) -> treelite.Model`. It must: (a) call `json.loads(ir_json)` inside a try/except `json.JSONDecodeError` that raises `ValueError`; (b) call `_validate_ir_dict(ir)`; (c) determine the task type from `ir["task"]` — the `task` field is a serde-serialized `Task` enum (from `config.rs`), which serializes as `{"Classification": {"n_classes": N}}` or `"Regression"` — handle both forms; (d) build the `ModelBuilder` with `threshold_type="float32"`, `leaf_output_type="float32"`, the correct `Metadata`, `TreeAnnotation`, `PostProcessorFunc(name="identity")` (per D-03, locked), and `base_scores=[0.0] * num_class`; (e) walk trees in a `for t in range(n_trees)` loop, computing `start = tree_offsets[t]` and `end = tree_offsets[t+1]`, calling `builder.start_tree()`, then for each `global_id` in `range(start, end)` compute `local_id = global_id - start` (D-04 mandatory reindex), call `builder.start_node(local_id)`, branch on `is_leaf[global_id]`, call `builder.leaf(...)` or `builder.numerical_test(...)`, then `builder.end_node()`, then `builder.end_tree()`; (f) return `builder.commit()`.

    For classifier leaves: multi-class uses `builder.leaf(list(leaf_proba[o*nc:(o+1)*nc]))` where `o = leaf_offset[global_id]` and `nc = n_classes`; binary (n_classes == 2) uses `kMultiClf` with `num_class=[2]` and the same 2-element vector leaf `[P(class=0), P(class=1)]` — do NOT use kBinaryClf with sigmoid (per D-03 and RESEARCH.md Pitfall 2).

    For internal nodes: `opname="<"` always (per D-03); `left_child_key = left_child[global_id] - start`; `right_child_key = right_child[global_id] - start`; `default_left = (default_child[global_id] == left_child[global_id])`.

    Use `builder.data_count(int(node_sample_count[global_id]))` and `builder.sum_hess(float(node_weighted_count[global_id]))` for internal nodes if the ForestIR JSON contains those fields (check with `.get()` — do not raise if missing, they are optional metadata per RESEARCH.md Pattern 1 row "node_sample_count").

    Wrap the entire `builder.*` block in a `try/except (KeyError, IndexError) as e: raise ValueError(...)` and a broader `except Exception as e: raise RuntimeError(...)` mirroring the `sylva_error_to_pyerr` pattern from `pyseam.rs` lines 46-53.

    Add `__all__ = ["export_to_treelite"]` at the top of the module.

    Do NOT fenced-code anything in this action — `export.py` is implementation; the patterns from PATTERNS.md supply the structure.
  </action>
  <verify>
    <automated>pip show treelite tl2cgen && python -c "from sylva.export import export_to_treelite; print('export_to_treelite importable')"</automated>
  </verify>
  <done>treelite 4.7.0 and tl2cgen 1.0.0 installed; `python/sylva/export.py` exists with `export_to_treelite()` and `_validate_ir_dict()`; the module-level docstring records the `import_from_json` spike result (exists or not) and the locked ModelBuilder decision.</done>
</task>

<task type="auto">
  <name>Task 2: Create conftest fixtures and parity test stubs (EXP-01, EXP-02)</name>
  <files>python/tests/conftest.py, python/tests/test_export.py</files>
  <read_first>
    - .planning/phases/09-treelite-export-packaging/09-PATTERNS.md (Pattern: "python/tests/conftest.py" — session-scoped fixture pattern lines 131-145; "python/tests/test_export.py" — round-trip parity test pattern lines 149-176; "Shared Patterns: Session-Scoped Expensive Fixtures" and "np.testing.assert_allclose Parity Assert")
    - python/tests/parity/conftest.py (existing clf_dataset, reg_dataset, et_clf_params session fixtures — check what is already defined to avoid re-defining)
    - crates/sylva-core/src/pyseam.rs (the fit_cpu / predict_cpu pyfunction names and signatures at lines 228-355)
  </read_first>
  <action>
    Update `python/tests/conftest.py` to add two session-scoped fixtures for the export tests:
    - `trained_clf_ir_small`: calls `sylva_core_pyseam.fit_cpu(X_train, y_train, {"n_estimators": 50, "max_depth": 6, "algo": "et", "seed": 42})` on a small make_classification dataset (500 samples, 10 features, 2 classes — use `sklearn.datasets.make_classification` with fixed `random_state=42` and dtype cast to float32); returns the opaque JSON string. Use `scope="session"` to avoid re-training across tests.
    - `trained_reg_ir_small`: same setup but `make_regression(n_samples=500, n_features=10, random_state=42)` cast to float32 targets, `criterion="mse"`, `n_estimators=50`, `max_depth=6`.
    - Add a fixture `clf_X_test_small` and `reg_X_test_small` (session-scoped, 100-row test arrays drawn from the same make_*() generator but at an offset, or use a fixed `np.random.default_rng(0).random((100, 10), dtype=float32)`) — small and deterministic so TL2cgen compilation stays fast per RESEARCH.md Pitfall 4.
    - Do not duplicate any existing fixtures already defined in `python/tests/parity/conftest.py`; import or reference them if available at the top-level scope.

    Create `python/tests/test_export.py` with the following test functions:
    - `test_export_builds(trained_clf_ir_small)`: calls `export_to_treelite(trained_clf_ir_small)` and asserts the returned object is a `treelite.Model` instance with the correct num_feature and n_trees metadata (access via `tl_model.num_feature` and `tl_model.num_tree` if those attributes exist in treelite 4.7 — check at runtime with `dir(tl_model)` in the test and assert whichever attribute exposes these).
    - `test_gtil_round_trip_clf(trained_clf_ir_small, clf_X_test_small)`: calls `export_to_treelite(trained_clf_ir_small)`, then `treelite.gtil.predict(tl_model, clf_X_test_small)`, then `sylva_core_pyseam.predict_cpu(trained_clf_ir_small, clf_X_test_small)`, and asserts `np.testing.assert_allclose(sylva_proba, tl_proba, atol=1e-5, err_msg="gtil round-trip clf: postprocessor mismatch? Check identity vs softmax")`.
    - `test_gtil_round_trip_reg(trained_reg_ir_small, reg_X_test_small)`: same pattern for regression; `predict_cpu` returns shape `(n, 1)` per pyseam.rs docstring — compare `sylva_pred.squeeze()` to `tl_pred.squeeze()` with `atol=1e-5`.
    - Add a `pytest.mark.slow` marker to `test_gtil_round_trip_clf` and `test_gtil_round_trip_reg` so they can be isolated from quick smoke runs.
    - Add `test_export_metadata(trained_clf_ir_small)`: parses the IR JSON directly (`json.loads(trained_clf_ir_small)`) and asserts that the exported treelite.Model has the same `n_trees` and `n_features` as the IR dict.
    - Import guard: at the top of the file, `import pytest; treelite = pytest.importorskip("treelite")` so the file is skipped cleanly if treelite is not installed (relevant for the clean-venv smoke plan).

    The tests may be RED at this wave if `export_to_treelite()` is not yet fully correct — that is acceptable; the spike's purpose is to run them and observe where they fail. The test file must be syntactically valid and importable.
  </action>
  <verify>
    <automated>python -m pytest python/tests/test_export.py --collect-only -q 2>&1 | grep -E "test_export|test_gtil" && echo "collection OK"</automated>
  </verify>
  <done>conftest.py has session-scoped `trained_clf_ir_small` and `trained_reg_ir_small` fixtures using 50-tree, depth-6 forests; test_export.py has at least four tests that collect without import errors; tests may be RED — the spike observes the failure mode to confirm postprocessor semantics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run spike parity tests and commit postprocessor decision (EXP-01 blocking gate)</name>
  <files>python/sylva/export.py</files>
  <action>
    Run the parity tests and observe results to confirm the postprocessor decision before Wave 1 proceeds. If test_gtil_round_trip_clf fails with a parity mismatch, adjust the postprocessor in export.py (try the alternative described in how-to-verify) and re-run until passing or until the failure mode is understood and documented.
  </action>
  <what-built>
    Tasks 1-2 created `export_to_treelite()` with `postprocessor="identity"` and `kMultiClf` with vector leaves for all classifiers, plus the round-trip parity tests. The tests are now runnable.
  </what-built>
  <how-to-verify>
    1. Run: `python -m pytest python/tests/test_export.py -x -q --tb=short`
    2. Observe the output:
       - If `test_gtil_round_trip_clf` passes with atol=1e-5: postprocessor="identity" is correct — proceed to Wave 1 (approved).
       - If `test_gtil_round_trip_clf` fails with allclose mismatch: note the predicted values from treelite.gtil vs sylva predict_cpu; try `postprocessor="softmax"` in export.py and re-run; record which works.
       - If `test_export_builds` fails: look for a ModelBuilder API error — likely a task_type or leaf_vector_shape argument issue; fix and re-run.
    3. Check `python/sylva/export.py` line 1-15 for the spike result comment recording:
       (a) whether `import_from_json` exists in treelite 4.7, and (b) the confirmed postprocessor.
    4. Confirm `git diff --stat` shows only the three planned files modified.
  </how-to-verify>
  <verify>
    <automated>python -m pytest python/tests/test_export.py::test_export_builds python/tests/test_export.py::test_gtil_round_trip_clf -q --tb=short</automated>
  </verify>
  <done>test_gtil_round_trip_clf passes or failure mode is identified and documented in export.py; postprocessor decision committed to the module-level docstring; Wave 1 can proceed with confirmed semantics.</done>
  <resume-signal>
    Type "approved" if test_gtil_round_trip_clf and test_gtil_round_trip_reg both pass (or nearly pass with an identified fix). Describe any failures so Wave 1 can correct export.py before full implementation. Paste the pytest output summary.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ir_json string → export_to_treelite() | Untrusted (in production); validated at function entry before any ModelBuilder call |
| treelite.gtil.predict() input | Accepts numpy array; treelite handles its own input validation |
| pip install (spike) | Installing treelite 4.7.0 and tl2cgen 1.0.0 from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Malformed ForestIR JSON passed to export_to_treelite() | mitigate | `_validate_ir_dict()` checks required keys and array length invariants (n_trees+1 == len(tree_offsets)) before any ModelBuilder call; raises ValueError with descriptive message |
| T-09-02 | Tampering | Out-of-range child indices (global IDs not reindexed) | mitigate | Mandatory `local_id = global_id - start` reindex per D-04; a wrong index causes ModelBuilder to raise rather than silently corrupt the model |
| T-09-SC | Tampering | pip install treelite==4.7.0 tl2cgen==1.0.0 | mitigate | Both packages cleared in RESEARCH.md Package Legitimacy Audit as SUS false positives (official DMLC/RAPIDS projects, Apache 2.0, multiple-year track record); versions pinned; no [ASSUMED] packages |
</threat_model>

<verification>
- `pip show treelite tl2cgen` shows 4.7.0 and 1.0.0 respectively.
- `python -c "from sylva.export import export_to_treelite"` exits 0.
- `python -m pytest python/tests/test_export.py --collect-only -q` collects at least 4 tests without import errors.
- `python/sylva/export.py` docstring records the `import_from_json` spike result and the locked postprocessor decision.
- Human checkpoint approved: `test_gtil_round_trip_clf` passes (or failure mode identified and fix committed for Wave 1).
</verification>

<success_criteria>
- EXP-01 prerequisite: `export_to_treelite()` exists with ModelBuilder implementation, `_validate_ir_dict()` validates at boundary, spike decision record committed.
- EXP-02 prerequisite: `test_gtil_round_trip_clf` and `test_gtil_round_trip_reg` are defined, runnable, and either GREEN or with a known-fix failure mode documented.
- Blocked unknowns resolved: import_from_json existence confirmed; postprocessor semantics confirmed empirically — Wave 1 can implement confidently.
</success_criteria>

<output>
Create `.planning/phases/09-treelite-export-packaging/09-01-SUMMARY.md` when done
</output>
