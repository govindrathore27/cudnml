---
phase: 09-treelite-export-packaging
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - python/tests/test_export.py
  - pyproject.toml
  - scripts/import_smoke_09.py
  - docs/INSTALL.md
autonomous: false
requirements:
  - EXP-02
  - EXP-03
user_setup:
  - service: clean-venv-wheel-smoke
    why: "EXP-03 requires a fresh Python venv with ONLY the wheel installed — no repo source on PYTHONPATH"
    env_vars: []
    dashboard_config:
      - task: "After Task 2 builds the wheel, create a fresh venv: py -m venv .venv-smoke-09 && .venv-smoke-09\\Scripts\\pip install target\\wheels\\sylva-*.cp310-abi3-win_amd64.whl && .venv-smoke-09\\Scripts\\python scripts\\import_smoke_09.py"
        location: "Terminal in the project root"
  - service: fil-wsl2-throughput-benchmark
    why: "FIL/nvForest GPU inference is Linux/WSL2 only — not testable in native Windows CI"
    env_vars: []
    dashboard_config:
      - task: "In WSL2 with RAPIDS installed: pip install treelite==4.7.0 && python tests/test_export.py::test_fil_round_trip — record agreement + throughput rows/s"
        location: "WSL2 terminal with cuML/nvForest/RAPIDS environment"

must_haves:
  truths:
    - "The TL2cgen compiled-CPU round-trip test passes: tl2cgen.create_shared('msvc', tmp_path) produces a .dll and tl2cgen.Predictor.predict() matches sylva predict_cpu within atol=1e-5 for both clf and reg"
    - "maturin build --release produces a cp310-abi3-win_amd64 wheel (or the wheel name expected by the installed Python version, using the Phase-1 proven abi3 config)"
    - "In a fresh .venv-smoke-09 with ONLY the wheel installed, import sylva succeeds and ExtraTreesClassifier() can be instantiated without error"
    - "docs/INSTALL.md documents the CUDA runtime prerequisites (NVIDIA driver, CUDA Toolkit 12.x, CUDA DLLs on PATH) and the three-step install path"
    - "scripts/import_smoke_09.py follows the Phase-1 import_smoke.py pattern exactly and exits 0 in the clean venv"
    - "The inference throughput comparative study (gtil rows/s vs sylva native predict_cpu rows/s vs sklearn predict rows/s) is recorded in docs/INSTALL.md or a study note under .planning/"
  prohibitions:
    - "MUST NOT bundle CUDA DLLs in the wheel — cudarc dynamic-loading resolves them at runtime from the user's CUDA Toolkit installation (wheel bloat + license violation)"
    - "MUST NOT attempt to run FIL/nvForest tests in native Windows CI — RAPIDS requires Linux or WSL2; gate behind checkpoint:human-verify"
    - "MUST NOT change pyproject.toml's cudarc feature from dynamic-loading to the static cuda-12080 launch-proof feature — dynamic-loading is the D-02 shipping config"
    - "MUST NOT skip the pyproject.toml name update from sylva-cuda to sylva if Phase 5 has renamed the package — verify current pyproject.toml state first"
  artifacts:
    - path: "python/tests/test_export.py"
      provides: "TL2cgen compiled-CPU round-trip tests and inference throughput benchmark"
      contains: "test_tl2cgen_round_trip"
    - path: "pyproject.toml"
      provides: "Updated maturin abi3+dynamic-loading config for the full sylva package"
      contains: "maturin"
    - path: "scripts/import_smoke_09.py"
      provides: "Clean-venv import smoke script (EXP-03)"
      contains: "import sylva"
    - path: "docs/INSTALL.md"
      provides: "Install path + CUDA prerequisite documentation; throughput study results"
      contains: "CUDA"
  key_links:
    - from: "scripts/import_smoke_09.py"
      to: "pyproject.toml"
      via: "wheel built by maturin from pyproject.toml; installed in clean venv; smoke script imports it"
      pattern: "import sylva"
    - from: "python/tests/test_export.py"
      to: "tl2cgen"
      via: "tl2cgen.generate_c_code() + create_shared('msvc') + Predictor.predict() — secondary round-trip path"
      pattern: "test_tl2cgen"
---

<objective>
Deliver EXP-02 (secondary path: TL2cgen compiled-CPU round-trip) and EXP-03 (abi3 Windows wheel in clean environment) plus the Comparative Baseline Study throughput report.

This plan completes the three-part phase deliverable:
1. TL2cgen compiled path: `tl2cgen.generate_c_code()` + `create_shared("msvc")` + `Predictor.predict()` matches Sylva predict_cpu — proves the model is portable to compiled CPU inference, not just gtil.
2. abi3 Windows wheel: generalize the Phase-1 proof to the full `sylva` package; a clean-venv `import sylva` + `ExtraTreesClassifier()` is the EXP-03 gate.
3. Throughput study: report inference rows/s for gtil / TL2cgen compiled / Sylva native predict_cpu / sklearn predict — with accuracy parity reported alongside. FIL/nvForest GPU benchmark gated behind a WSL2 human checkpoint.
4. INSTALL.md: documents CUDA runtime prerequisites and the three-step install path for end users.

Purpose: A user who runs `pip install sylva` and `pip install treelite tl2cgen` can export their trained model and run fast compiled inference. The wheel installs cleanly and does not require CUDA at import time.

Output: TL2cgen round-trip test GREEN, abi3 wheel builds + clean-venv smoke passes, INSTALL.md, throughput study numbers recorded.
</objective>

## Assumptions

| # | Decision | Implementation |
|---|----------|----------------|
| D-05 (EXP-03) | abi3 wheel + cudarc dynamic-loading per Phase-1 proof | pyproject.toml update mirrors Phase-1 01-03-PLAN.md wheel config; `dynamic-loading` feature retained |
| D-02 | Export Python module, no new PyO3 binding needed | TL2cgen test is pure Python; pyseam.rs unchanged in this plan |
| D-06 (EXP-02 secondary) | TL2cgen is the Windows CI compiled-inference path | `create_shared("msvc", ...)` — MSVC cl.exe from VS 2022 Build Tools already on PATH from Phase 1 |
| FIL platform | FIL/nvForest is Linux/WSL2 only — manual checkpoint | Not in Windows CI; throughput from WSL2 manual run documented in INSTALL.md |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-treelite-export-packaging/09-RESEARCH.md
@.planning/phases/09-treelite-export-packaging/09-PATTERNS.md
@.planning/phases/09-treelite-export-packaging/09-02-SUMMARY.md
@pyproject.toml
@.planning/phases/01-toolchain-spike-gate-1/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TL2cgen compiled round-trip test and inference throughput study (EXP-02)</name>
  <files>python/tests/test_export.py</files>
  <read_first>
    - .planning/phases/09-treelite-export-packaging/09-RESEARCH.md (Pattern 2 "Round-Trip CI Test" TL2cgen secondary path code example; Pitfall 3 FIL on Windows; Pitfall 4 TL2cgen MSVC compilation timeout — use small forests; Comparative Baseline Study metrics: rows/s for gtil / TL2cgen / native / sklearn)
    - .planning/phases/09-treelite-export-packaging/09-PATTERNS.md ("python/tests/test_export.py" section — TL2cgen compiled-path test pattern lines 163-176; FIL gate pattern lines 180-186; "np.testing.assert_allclose Parity Assert" shared pattern)
    - python/tests/test_export.py (current test file from Plan 02 — read before editing to understand what already exists)
    - .planning/ROADMAP.md (Phase 9 Comparative Baseline Study section: inference throughput gtil/TL2cgen vs Sylva native predict vs sklearn predict; fairness protocol applies — agreement reported alongside speed; repeated runs; versions pinned)
  </read_first>
  <action>
    Add three functions to `python/tests/test_export.py`:

    `test_tl2cgen_round_trip_clf(trained_clf_ir_small, clf_X_test_small, tmp_path)`:
    - Imports `tl2cgen` inside the function (avoids SkipError if tl2cgen absent).
    - Calls `export_to_treelite(trained_clf_ir_small)` to get the treelite.Model.
    - Calls `tl2cgen.generate_c_code(tl_model, str(tmp_path), params={})`.
    - Calls `libpath = tl2cgen.create_shared("msvc", str(tmp_path))` — produces a .dll on Windows.
    - Creates `predictor = tl2cgen.Predictor(libpath)`.
    - Calls `tl2cgen.DMatrix(clf_X_test_small)` and `predictor.predict(dmat)`.
    - Asserts `np.testing.assert_allclose(sylva_proba, tl2cgen_pred, atol=1e-5, err_msg="TL2cgen clf round-trip: check postprocessor or node reindex")`.
    - Mark `@pytest.mark.slow` to allow `pytest -m "not slow"` to skip it in fast CI runs.

    `test_tl2cgen_round_trip_reg(trained_reg_ir_small, reg_X_test_small, tmp_path)`:
    - Same structure for regression.
    - `predict_cpu` returns shape `(n,1)` — use `.squeeze()` on both sylva_pred and tl2cgen_pred before the allclose assert.
    - Mark `@pytest.mark.slow`.

    `test_inference_throughput(trained_clf_ir_small, clf_X_test_small)`:
    - NOT a correctness gate — this is the Comparative Baseline Study throughput measurement.
    - Mark `@pytest.mark.benchmark` (or `@pytest.mark.slow`).
    - Measure rows/s for three paths on a larger batch (e.g., `np.tile(clf_X_test_small, (100, 1))` to get 10,000 rows):
      a. Sylva native: `sylva_core_pyseam.predict_cpu(ir, X_large)` — time 10 repeated calls, take median.
      b. treelite.gtil: `treelite.gtil.predict(tl_model, X_large)` — time 10 repeated calls, take median.
      c. sklearn reference: `sklearn.ensemble.ExtraTreesClassifier(n_estimators=50, max_depth=6, random_state=42).fit(X_train, y_train).predict_proba(X_large)` — time 10 repeated calls, take median.
    - Use `time.perf_counter()` or `timeit.repeat()` for timing; report warmup (discard first 2 runs) and median of remaining.
    - Print the results as a table with `print(f"Inference throughput on {len(X_large)} rows...")` — this test does NOT assert any speed gate; it only asserts agreement: `np.testing.assert_allclose(sylva_proba, gtil_proba, atol=1e-5)` to confirm parity alongside speed.
    - The test does NOT assert throughput order (Sylva native faster than gtil, etc.) — throughput is reported, not gated (per Phase 9 Comparative Baseline Study and fairness protocol PITFALLS.md).
    - Add `@pytest.mark.skip(reason="manual benchmark run only")` so it is not run in standard CI — the throughput numbers are produced by a manual `pytest -m benchmark` invocation and pasted into INSTALL.md.

    Add a FIL gate stub (does NOT run in Windows CI):
    `test_fil_round_trip_wsl2_only(trained_clf_ir_small, clf_X_test_small)`:
    - Body: `pytest.skip("FIL/nvForest requires Linux/WSL2 — run manually in WSL2 with RAPIDS installed")`.
    - Mark `@pytest.mark.linux`.
    - This stub documents the FIL path without attempting to import cuml or nvforest in the Windows CI environment.

    Add a `pytest.ini_options` section to `pyproject.toml` (or create `pytest.ini`) registering the custom markers: `slow`, `benchmark`, `linux` — without this, pytest warns about unknown markers.
  </action>
  <verify>
    <automated>python -m pytest python/tests/test_export.py::test_tl2cgen_round_trip_clf python/tests/test_export.py::test_tl2cgen_round_trip_reg -q --tb=short</automated>
  </verify>
  <done>test_tl2cgen_round_trip_clf and test_tl2cgen_round_trip_reg both pass (GREEN); EXP-02 TL2cgen compiled-CPU path verified; inference throughput benchmark test stub committed; FIL gate stub present but skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize abi3 wheel and write INSTALL.md (EXP-03)</name>
  <files>pyproject.toml, scripts/import_smoke_09.py, docs/INSTALL.md</files>
  <read_first>
    - pyproject.toml (current contents — read the full file; check if it still points to sylva-cuda or has been updated by Phase 5; determine if manifest-path and module-name need updating)
    - .planning/phases/09-treelite-export-packaging/09-PATTERNS.md ("pyproject.toml update" section — proven config to copy; key constraint: `features = ["pyseam"]` MUST NOT be set in wheel build; dynamic-loading feature is D-02 shipping config)
    - .planning/phases/01-toolchain-spike-gate-1/01-03-PLAN.md (Task 1 wheel build details — PYO3_USE_ABI3_FORWARD_COMPATIBILITY env var; interpreter fallback pattern; the exact maturin command that produced the cp310-abi3-win_amd64 wheel)
    - .planning/phases/01-toolchain-spike-gate-1/01-03-SUMMARY.md (whether PYO3_USE_ABI3_FORWARD_COMPATIBILITY was needed; the exact command that worked; VERSIONS.md kill-decision was "proceed")
    - .planning/phases/09-treelite-export-packaging/09-RESEARCH.md (Pattern 3 abi3 Windows Wheel Validation; CUDA as runtime prerequisite; §Environment Availability — Python 3.14.3, maturin 1.14.1, Rust 1.96.0)
  </read_first>
  <action>
    Read `pyproject.toml` first. Determine whether Phase 5 has already updated it to point to the full estimator crate or if it still points to `sylva-cuda`. Apply the minimum necessary update:

    The `[project]` section:
    - `name = "sylva"` (rename from "sylva-cuda" if not already done — per PATTERNS.md pyproject.toml pattern).
    - `description = "GPU-native forest ensembles — Extra Trees and Random Forest (Sylva)."` (update if needed).
    - `requires-python = ">=3.10"` (unchanged).
    - `license = { text = "Apache-2.0" }` (unchanged).
    - `dynamic = ["version"]` (unchanged — version sourced from the crate).
    - `dependencies = ["numpy>=1.25"]` (add this; treelite and tl2cgen are optional serving dependencies, not required for `import sylva`).

    The `[tool.maturin]` section:
    - `manifest-path`: if Phase 5 has a unified crate, point to it; otherwise keep pointing to `crates/sylva-core/Cargo.toml` which has the `pyseam` feature (the pyseam feature MUST NOT be included in the wheel build — verify the default feature set does not include `pyseam`).
    - `module-name = "sylva"` — the PyO3 module must be named `sylva` to match `import sylva`.
    - Do NOT add `features = ["pyseam"]` to the `[tool.maturin]` section — pyseam is test-only.
    - Ensure the cudarc feature in the crate's default features is `dynamic-loading` (not `cuda-static`) for the wheel build — this is the Phase-1 proven D-02 shipping config. Do NOT change this.
    - Add `[tool.pytest.ini_options]` section at the bottom with `markers = ["slow: marks tests as slow", "benchmark: marks throughput benchmarks", "linux: marks Linux-only tests"]` if not already present (avoids pytest marker warnings from Task 1).

    Create `scripts/import_smoke_09.py` following the Phase-1 `scripts/import_smoke.py` pattern exactly:
    - `import sys; print(f"Python: {sys.version}")`
    - `import sylva; print(f"sylva: {sylva.__version__}"); print(f"sylva location: {sylva.__file__}")`
    - `e = sylva.ExtraTreesClassifier(); print(f"ExtraTreesClassifier(): {e}")`
    - `print("OK — sylva imported and ExtraTreesClassifier instantiated in clean venv")`
    - Exit 0 on success; any ImportError or AttributeError will cause a non-zero exit.
    - Do NOT call any GPU method or import treelite in this script — the clean-venv may not have treelite installed, and CUDA dynamic-loading means GPU init is deferred to first use (not import time).

    Run `maturin build --release` to produce the wheel. If `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1` is needed (per Phase-1 SUMMARY), set it before the build command. Record the exact command that succeeds in INSTALL.md.

    Create `docs/INSTALL.md` with these sections:
    - Prerequisites: NVIDIA GPU; NVIDIA driver supporting CUDA 12.x or 13.x; CUDA Toolkit 12.x installed; `nvcuda.dll` / CUDA runtime DLLs on PATH (Windows) or LD_LIBRARY_PATH (Linux).
    - Quick Install: `pip install sylva` (when published); for development: `maturin develop` from the repo root.
    - Optional Serving Stack: `pip install treelite==4.7.0 tl2cgen==1.0.0` for compiled CPU inference; nvForest (Linux/WSL2 only) for GPU inference via RAPIDS.
    - Platform Notes: CUDA DLLs resolved at runtime via cudarc `dynamic-loading` — `import sylva` succeeds even without a GPU present; GPU methods fail with a clear error if no CUDA device is found. FIL/nvForest requires Linux or WSL2 (RAPIDS does not ship Windows wheels).
    - Validation: `python -c "import sylva; print(sylva.__version__)"` should print the version and exit 0.
    - Throughput Study (Comparative Baseline Study results): a table of inference throughput (rows/s) for gtil / TL2cgen compiled / Sylva native predict_cpu / sklearn predict on the 10,000-row clf batch, from the `test_inference_throughput` manual run. Include agreement (parity confirmed: atol=1e-5) alongside the speed numbers per the fairness protocol. Note: TL2cgen compiled inference may be faster than gtil (gtil is pure Python), and sklearn predict on a pre-trained model is very fast for small forests — report honestly, including where Sylva native predict is slower. Note the hardware (RTX 4060 Ti, driver version, CUDA toolkit version).
    - FIL/nvForest GPU Inference (Linux/WSL2 manual benchmark): a placeholder section noting the benchmark should be run in WSL2 with RAPIDS and the results pasted here after the human-verify checkpoint below.
  </action>
  <verify>
    <automated>python -m pytest python/tests/test_export.py -q --ignore=python/tests/test_export.py -k "not benchmark and not linux" && maturin build --release 2>&1 | grep -i "abi3\|win_amd64\|wheel" | tail -3</automated>
  </verify>
  <done>pyproject.toml updated with `name = "sylva"` and `dependencies = ["numpy>=1.25"]`; `maturin build --release` produces a cp310-abi3-win_amd64 wheel; `scripts/import_smoke_09.py` created; `docs/INSTALL.md` written with CUDA prerequisites + throughput study template.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Clean-venv wheel smoke (EXP-03) and FIL/WSL2 benchmark gate</name>
  <files>docs/INSTALL.md</files>
  <action>
    Run the clean-venv smoke test per how-to-verify steps. If Part A fails, diagnose the cause (module name mismatch, static CUDA linking, missing PyO3 abi3 config) and fix pyproject.toml or the Rust crate before re-running `maturin build --release`. If Part B (FIL/WSL2) is available, run the nvForest benchmark and paste results into docs/INSTALL.md.
  </action>
  <what-built>
    Tasks 1-2 built the wheel (`maturin build --release`), created `scripts/import_smoke_09.py`, and wrote `docs/INSTALL.md`. The TL2cgen round-trip tests are GREEN (Task 1). Now the wheel must be validated in a genuinely fresh Python environment.
  </what-built>
  <how-to-verify>
    Part A — Clean-venv smoke (EXP-03):
    1. Create a fresh venv: `py -m venv .venv-smoke-09` (using the system Python, NOT the dev environment).
    2. Install ONLY the wheel: `.venv-smoke-09\Scripts\pip install target\wheels\sylva-*.cp310-abi3-win_amd64.whl`
    3. Do NOT install any dev dependencies (no treelite, no sylva_core_pyseam, no pytest).
    4. Run: `.venv-smoke-09\Scripts\python scripts\import_smoke_09.py`
    5. Expected output:
       - A line showing `sylva version: X.Y.Z`
       - A line showing `sylva location: ...\.venv-smoke-09\Lib\site-packages\sylva...` (MUST be inside the venv, not the repo source)
       - `ExtraTreesClassifier():` followed by the repr
       - `OK — sylva imported and ExtraTreesClassifier instantiated in clean venv`
       - Exit code 0
    6. If import fails: check that `pyproject.toml module-name = "sylva"` matches the PyO3 module name in the Rust code; check that `maturin build --release` used dynamic-loading and not static CUDA.

    Part B — FIL/nvForest throughput benchmark (optional, Linux/WSL2 only):
    If you have a WSL2 environment with RAPIDS/cuML installed:
    1. In WSL2: `pip install treelite==4.7.0 && pip install sylva` (or build and install the Linux wheel).
    2. Copy the trained_clf_ir JSON string from a Python session to WSL2 (or re-train a small forest).
    3. Run: `python -c "import treelite, nvforest, json, numpy as np; ..."` (adapt from test_fil_round_trip stub in test_export.py).
    4. Record rows/s and agreement (atol=1e-5 vs sylva predict). Paste the result into `docs/INSTALL.md` FIL/nvForest section.
    If WSL2/RAPIDS is not available: skip Part B and note "FIL benchmark deferred — WSL2 environment not available" in docs/INSTALL.md. EXP-02 is satisfied by gtil + TL2cgen (Part A of the CI tests).
  </how-to-verify>
  <verify>
    <automated>python -m pytest python/tests/test_export.py -q -m "not slow and not benchmark and not linux" && test -f target/wheels/sylva-*.whl && echo "wheel exists"</automated>
  </verify>
  <done>Clean-venv `scripts/import_smoke_09.py` exits 0 with sylva location inside the venv; EXP-03 complete. FIL/nvForest throughput results recorded in docs/INSTALL.md or deferred with an honest note.</done>
  <resume-signal>
    Type "approved" once the clean-venv smoke passes (Part A green) with exit code 0 and the sylva location inside the venv. Paste the import_smoke_09.py output. Optionally paste the FIL throughput numbers if WSL2 was available. Describe any failures.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install <wheel> in clean venv | The wheel is self-built; only sylva wheel installed, no external packages |
| tl2cgen.create_shared("msvc", tmp_path) | Invokes cl.exe on MSVC-generated C code; tmp_path is a pytest-managed tmpdir (not user-controlled) |
| FIL/nvForest WSL2 benchmark | Manual step; not in CI; human-controlled environment |
| treelite.Model loaded from export_to_treelite | In-memory, built from validated ForestIR — not loading from an untrusted file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | tl2cgen.create_shared loads arbitrary .dll from tmp_path | mitigate | Use pytest `tmp_path` fixture (a process-scoped temp dir managed by pytest, not user-accessible in CI); do not call `Predictor(arbitrary_user_path)` — only the tl2cgen-generated .dll |
| T-09-07 | Tampering | wheel bundling: cudarc static linking could embed CUDA symbols | mitigate | pyproject.toml must use `dynamic-loading` cudarc feature (D-02); verified by `maturin build --release` log showing no embedded CUDA; `import sylva` smoke in clean venv without CUDA Toolkit would fail if static was accidentally linked |
| T-09-08 | Information Disclosure | INSTALL.md may mention internal Rust crate structure | accept | INSTALL.md is documentation for end users; no secrets, no PII; developer-level detail is acceptable |
| T-09-09 | Denial of Service | TL2cgen MSVC compilation of large forest CI timeout (Pitfall 4) | mitigate | Use only the small trained_clf_ir_small and trained_reg_ir_small fixtures (50 trees, max_depth=6) in tl2cgen test; no production-size forest in CI |
| T-09-SC | Tampering | No new pip installs in this plan (treelite/tl2cgen already from Wave 0) | accept | No new supply chain exposure; previously cleared packages |
</threat_model>

<verification>
- `python -m pytest python/tests/test_export.py::test_tl2cgen_round_trip_clf python/tests/test_export.py::test_tl2cgen_round_trip_reg -q` both GREEN.
- `python -m pytest python/tests/test_export.py -q -m "not slow and not benchmark and not linux"` — all non-slow tests GREEN.
- `maturin build --release` produces `target/wheels/sylva-*.cp310-abi3-win_amd64.whl`.
- Human checkpoint Part A approved: `scripts/import_smoke_09.py` exits 0 in a clean venv showing sylva location inside the venv.
- `docs/INSTALL.md` exists and contains the word "CUDA" (runtime prerequisite section present).
- `scripts/import_smoke_09.py` exists and contains `import sylva`.
- No CUDA DLL bundled in the wheel (dynamic-loading verified by clean-venv import without CUDA Toolkit on PATH if possible).
</verification>

<success_criteria>
- EXP-02 DONE (secondary path): TL2cgen compiled-CPU round-trip matches Sylva predict_cpu within atol=1e-5 for clf and reg; FIL/nvForest gate documented as WSL2-only manual benchmark.
- EXP-03 DONE: abi3 Windows wheel (`cp310-abi3-win_amd64`) builds via `maturin build --release`; `import sylva` in a clean venv with ONLY the wheel installed exits 0; `ExtraTreesClassifier()` instantiates.
- Throughput study: inference rows/s for gtil / TL2cgen / Sylva native predict_cpu / sklearn predict recorded in `docs/INSTALL.md` with accuracy parity confirmed alongside speed.
- INSTALL.md: CUDA runtime prerequisites documented; three-step install path documented; FIL/nvForest platform limitation noted.
</success_criteria>

<output>
Create `.planning/phases/09-treelite-export-packaging/09-03-SUMMARY.md` when done
</output>
