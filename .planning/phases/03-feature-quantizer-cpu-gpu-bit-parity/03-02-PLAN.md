---
phase: 03-feature-quantizer-cpu-gpu-bit-parity
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/sylva-core/src/quantize/parity.rs
  - crates/sylva-core/src/quantize/mod.rs
  - crates/sylva-core/tests/quantize_golden.rs
  - crates/sylva-core/tests/fixtures/quantize_golden_v1.json
autonomous: true
requirements: [QUANT-02]
must_haves:
  truths:
    - "A serialized golden-vector fixture (seed, n_bins, X-with-NaNs, expected column-major bins, edges) exists on disk and round-trips through serde identically"
    - "The Rust CPU Quantizer reproduces the fixture's expected_bins bit-for-bit on the fixed seed (the QUANT-02 parity CONTRACT for this phase)"
    - "The fixture exercises: a generic dense block, a low-cardinality column (dedupe), a column with values exactly equal to edges (D-06 boundary), a NaN column (reserved missing bin), and out-of-range values (clamp)"
    - "The fixture metadata states the Phase-4 GPU requirement (the GPU-assigns-only half of D-04): literal v <= edge compare against the uploaded host edges, no FMA, assignment path compiled WITHOUT --use_fast_math (forward-carried contract)"
  artifacts:
    - path: "crates/sylva-core/src/quantize/parity.rs"
      provides: "GoldenVectors fixture struct + serde (de)serialize + load/save helpers; Phase-4 contract metadata"
      contains: "struct GoldenVectors"
    - path: "crates/sylva-core/tests/fixtures/quantize_golden_v1.json"
      provides: "The frozen QUANT-02 golden-vector fixture (the literal Phase-4 hand-off)"
      contains: "expected_bins"
    - path: "crates/sylva-core/tests/quantize_golden.rs"
      provides: "Integration test: load fixture, run Quantizer, assert CPU bins == expected_bins"
      contains: "quantize"
  key_links:
    - from: "crates/sylva-core/tests/quantize_golden.rs"
      to: "crates/sylva-core/src/quantize/parity.rs"
      via: "loads GoldenVectors from quantize_golden_v1.json"
      pattern: "GoldenVectors"
    - from: "crates/sylva-core/tests/quantize_golden.rs"
      to: "crates/sylva-core/src/quantize/mod.rs"
      via: "runs Quantizer::fit_transform on the fixture X and compares bins"
      pattern: "Quantizer"
  prohibitions:
    - "MUST NOT regenerate the golden vectors with edge math different from the Plan-01 A1-pinned cast point — the fixture is frozen against that exact edge math"
    - "MUST NOT assert the golden bins were produced by a live GPU run — this phase is a CONTRACT (golden vectors); the live CPU↔GPU CI proof is Phase 4 (D-05)"
    - "MUST NOT omit the no-FMA / no--use_fast_math metadata — the by-construction parity guarantee depends on the Phase-4 kernel staying a pure compare"
    - "MUST NOT use epsilon comparison when asserting CPU bins == expected_bins — exact integer/bit equality only"
---

<objective>
Freeze the QUANT-02 bit-parity contract as golden-vector fixtures and a Rust integration test that asserts the Plan-01 CPU `Quantizer` reproduces them exactly. This is the literal hand-off to Phase 4: the GPU assignment kernel must later reproduce the same `expected_bins` bit-for-bit.

Purpose: Satisfy SC-2 "as a contract" (D-05) — the live CPU↔GPU CI run is deferred to Phase 4, but the expected bins, the seed, the edges, and the no-FMA/no-fast-math requirement are pinned NOW so the Phase-4 kernel is testable on arrival. The vertical slice: a fixture in → an enforced parity assertion out.

Output: `crates/sylva-core/src/quantize/parity.rs` (fixture struct + serde), the frozen `quantize_golden_v1.json`, and `crates/sylva-core/tests/quantize_golden.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-CONTEXT.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-RESEARCH.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-PATTERNS.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-01-SUMMARY.md
@crates/sylva-core/src/parity.rs
@crates/sylva-core/src/ir.rs
@crates/sylva-core/src/quantize/mod.rs
</context>

<artifacts_this_phase_produces>
This plan creates (Phase-3 new symbols — excluded from any source-drift verification):
- `crates/sylva-core/src/quantize/parity.rs` with struct `GoldenVectors` (fields: `seed`, `n_bins`, `x_flat` + shape, `expected_bins` column-major u8, `edges_flat` + `edges_offsets`, `contract_note`) and serde (de)serialize + `load_from_str` / `to_json` helpers.
- Fixture file `crates/sylva-core/tests/fixtures/quantize_golden_v1.json` (the frozen contract).
- Integration test file `crates/sylva-core/tests/quantize_golden.rs`.
- `quantize/mod.rs` gains `mod parity;` (or `pub mod parity;`) and re-exports `GoldenVectors`.

Note: distinct from `crates/sylva-core/src/parity.rs` (the Phase-2 `SplitStats` module). This is `quantize/parity.rs` — same role/twin, different path.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: GoldenVectors fixture struct + serde + Phase-4 contract metadata (quantize/parity.rs)</name>
  <files>crates/sylva-core/src/quantize/parity.rs, crates/sylva-core/src/quantize/mod.rs</files>
  <read_first>
    - crates/sylva-core/src/parity.rs (lines 1-22 module doc "consumed by an out-of-Rust harness via serde_json" convention; lines 35-59 SplitStats serde-record shape; lines 294-304 serde round-trip test to replicate)
    - crates/sylva-core/src/ir.rs (line 22 the canonical derive line; the serde_round_trip test pattern)
    - crates/sylva-core/src/quantize/mod.rs (the Quantizer public surface + BinEdges layout to mirror in the fixture)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-RESEARCH.md (§Architecture Pattern 3 "Golden-vector parity fixture"; Pitfall 2 the no-FMA/no-fast-math contract; Pitfall 4 NaN fixture)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-PATTERNS.md (§parity.rs golden-vector twin)
  </read_first>
  <behavior>
    - `GoldenVectors` derives the canonical `#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]`; serde round-trip (`to_string` → `from_str`) is identity (assert_eq).
    - Stores X as a flat f32 vector + (n_rows, n_features); `expected_bins` as flat column-major u8; edges as CSR (`edges_flat` + `edges_offsets`); plus `seed`, `n_bins`, and a `contract_note: String`.
    - `contract_note` literally records the Phase-4 GPU requirement (pure `v <= edge` compare against uploaded host edges; no FMA; assignment path WITHOUT `--use_fast_math`).
  </behavior>
  <action>
    Create `crates/sylva-core/src/quantize/parity.rs`. Open with a `//!` module doc in the `crates/sylva-core/src/parity.rs:1-22` shape: state this is the QUANT-02 golden-vector contract consumed by Phase-4 CI via serde_json, and that for THIS phase SC-2 is satisfied as a contract (golden vectors), with the live CPU↔GPU run landing in Phase 4 (D-05). Define `pub struct GoldenVectors` with the canonical derive line and fields: `seed: u64`, `n_bins: usize`, `n_rows: usize`, `n_features: usize`, `x_flat: Vec<f32>` (row-major input), `expected_bins: Vec<u8>` (column-major), `edges_flat: Vec<f32>`, `edges_offsets: Vec<usize>`, `contract_note: String`. Add `///` doc on each field. Add helpers `to_json(&self) -> Result<String, SylvaError>` and `from_json(s: &str) -> Result<Self, SylvaError>` (map serde errors to `SylvaError::InvalidInput`). Add a `pub const PHASE4_CONTRACT: &str` constant holding the no-FMA / no--use_fast_math / pure-compare requirement text, used to populate `contract_note`.
    Add `#[cfg(test)]` serde round-trip test replicating `parity.rs:294-304` (`to_json` → `from_json` → `assert_eq!`). Add `mod parity;` + `pub use parity::{GoldenVectors, PHASE4_CONTRACT};` to `quantize/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p sylva-core quantize::parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p sylva-core quantize::parity` passes including the serde round-trip identity test.
    - `GoldenVectors` and `PHASE4_CONTRACT` are importable from `sylva_core::quantize`.
    - `PHASE4_CONTRACT` text contains the no-FMA and no--use_fast_math requirement (grep-checkable in Task 3's fixture).
  </acceptance_criteria>
  <done>GoldenVectors is a serde record twin of SplitStats holding the fixture + the forward-carried Phase-4 compute-contract metadata; serde round-trip is identity.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and freeze the golden-vector fixture JSON (quantize_golden_v1.json)</name>
  <files>crates/sylva-core/tests/fixtures/quantize_golden_v1.json, crates/sylva-core/tests/quantize_golden.rs (the #[ignore] generator fn)</files>
  <read_first>
    - crates/sylva-core/src/quantize/mod.rs (Quantizer::fit_transform — used to PRODUCE the expected_bins from the fixture X under the A1-pinned edge math)
    - crates/sylva-core/src/quantize/parity.rs (GoldenVectors shape to serialize)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-01-SUMMARY.md (the A1-pinned cast point + recorded numpy reference — the fixture must be frozen against this exact edge math)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-RESEARCH.md (§Architecture Pattern 3 fixture shape recommendation: 5 column kinds; Pitfall 4 carry NaN fixtures from Phase-2 SC-4)
  </read_first>
  <behavior>
    - The fixture X (small, e.g. ~16-32 rows × ~5 features) deliberately includes: (1) a generic dense column, (2) a low-cardinality column (< n_data_bins distinct → dedupe → fewer bins), (3) a column with values placed exactly at edge values (D-06 boundary exercise), (4) a column containing NaNs (reserved missing bin), (5) a column with out-of-range values relative to its own edges (clamp).
    - `expected_bins` is produced by running the Plan-01 `Quantizer` (NOT hand-typed) so it is internally consistent with the A1-pinned edge math; the test in Task 3 then re-derives and asserts equality (the fixture is the frozen oracle).
    - A small fixed `n_data_bins` (e.g. 4 or 8) so the fixture is human-auditable; `seed` recorded even though D-01 uses no RNG (carried for the Phase-4 fixed-seed contract framing).
  </behavior>
  <action>
    Write a one-shot generator (a `#[test]`-gated or `examples/`-style helper, or a `cargo test -- --ignored` generator function in `quantize_golden.rs`) that constructs the 5-kind fixture `X`, runs `Quantizer::fit_transform`, fills a `GoldenVectors { seed, n_bins, ..., expected_bins, edges_flat, edges_offsets, contract_note: PHASE4_CONTRACT.into() }`, and serializes it to `crates/sylva-core/tests/fixtures/quantize_golden_v1.json` via `to_json`. Run the generator ONCE, commit the JSON, then the generator stays `#[ignore]` (it documents provenance but the committed JSON is the frozen artifact). Ensure the JSON `contract_note` field contains the no-FMA / no--use_fast_math text from `PHASE4_CONTRACT`. Do not regenerate with different edge math (prohibition).
  </action>
  <verify>
    <automated>test -f crates/sylva-core/tests/fixtures/quantize_golden_v1.json && grep -q 'use_fast_math' crates/sylva-core/tests/fixtures/quantize_golden_v1.json && grep -q 'expected_bins' crates/sylva-core/tests/fixtures/quantize_golden_v1.json</automated>
  </verify>
  <acceptance_criteria>
    - `crates/sylva-core/tests/fixtures/quantize_golden_v1.json` exists and deserializes into `GoldenVectors` without error.
    - The JSON contains a NaN-bearing column whose cells map to the reserved missing bin in `expected_bins`.
    - The JSON `contract_note` contains the literal Phase-4 requirement (no FMA, no `--use_fast_math`, pure `v <= edge` compare) — grep `use_fast_math` succeeds.
    - The fixture covers all 5 column kinds (dense, low-cardinality, on-edge, NaN, out-of-range).
  </acceptance_criteria>
  <done>The frozen golden-vector fixture exists on disk, produced by the A1-pinned Quantizer, covering all 5 column kinds and carrying the Phase-4 no-FMA/no-fast-math contract note.</done>
</task>

<task type="auto">
  <name>Task 3: Golden-vector parity integration test (quantize_golden.rs)</name>
  <files>crates/sylva-core/tests/quantize_golden.rs</files>
  <read_first>
    - crates/sylva-core/tests/invariants.rs (the `tests/` integration-test layout + how it imports `sylva_core::`)
    - crates/sylva-core/src/quantize/parity.rs (GoldenVectors::from_json)
    - crates/sylva-core/src/quantize/mod.rs (Quantizer::fit_transform return shape)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-VALIDATION.md (the QUANT-02 row: `quantize::parity_golden` integration test = the bit-parity contract, golden fixtures not a live GPU run)
  </read_first>
  <behavior>
    - Loads `quantize_golden_v1.json`, reconstructs the fixture `X` (row-major from `x_flat` + shape), runs `Quantizer::fit_transform` with the fixture's `n_bins`, and asserts the produced column-major bins equal `expected_bins` by exact integer equality (NOT approx).
    - Also asserts the produced edges equal `edges_flat`/`edges_offsets` (the edge math is part of the contract).
    - Fails loudly if any single cell differs (reports the cell index, expected vs actual).
  </behavior>
  <action>
    Create `crates/sylva-core/tests/quantize_golden.rs`. Add a `#[test] fn parity_golden()` that: reads the fixture file via `std::fs::read_to_string` (path relative to `CARGO_MANIFEST_DIR`), `GoldenVectors::from_json`, rebuilds `ArrayView2<f32>` from `x_flat`/`n_rows`/`n_features`, runs `Quantizer::fit_transform`, extracts the u8 column-major bin slice, and `assert_eq!(produced_bins, golden.expected_bins)` (exact). Add a second assertion that the produced `BinEdges.flat == golden.edges_flat` and `offsets == golden.edges_offsets`. Keep the Task-2 generator function in this same file behind `#[ignore]` so provenance is co-located but does not run in the normal suite. Name the test so `cargo test -p sylva-core quantize::parity_golden` (or `--test quantize_golden`) selects it.
  </action>
  <verify>
    <automated>cargo test -p sylva-core --test quantize_golden parity_golden</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p sylva-core --test quantize_golden parity_golden` passes: CPU `Quantizer` bins == `expected_bins` bit-for-bit.
    - Mutating any single byte of `expected_bins` in a scratch copy makes the test fail (the assertion is exact, not approx) — verified by the executor as a sanity check, not committed.
    - The produced edges match the fixture edges exactly.
  </acceptance_criteria>
  <done>The QUANT-02 parity contract is enforced in CI: the CPU Quantizer reproduces the frozen golden bins and edges exactly, ready for the Phase-4 GPU kernel to assert against the same fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file → test harness | The committed JSON is trusted (in-repo, reviewed); deserialization still goes through typed `SylvaError` rather than `.unwrap()` on malformed JSON. |
| Phase-3 contract → Phase-4 kernel | The golden vectors + `contract_note` are the security-relevant hand-off: they constrain the future GPU kernel to a pure compare so QUANT-02 parity holds by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering (parity integrity) | Phase-4 assignment kernel (future) drifting from pure compare | mitigate | Embed the no-FMA / no-`--use_fast_math` / `v <= edge`-only requirement in `contract_note` and `PHASE4_CONTRACT`; the golden fixture is the bit-exact target the Phase-4 kernel must match. (high — this is the load-bearing QUANT-02 guarantee.) |
| T-03-06 | Tampering | `GoldenVectors::from_json` on malformed fixture | accept | Fixture is in-repo and reviewed; deserialize maps errors to typed `SylvaError` (no panic), but no external/untrusted fixture source exists, so residual risk is low. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (all deps pinned in Phase 2; RESEARCH §Package Legitimacy Audit confirms zero new packages). No `[ASSUMED]`/`[SUS]` packages → no legitimacy checkpoint required. |

Block-on severity: high. T-03-05 (parity-integrity contract) is the high-severity item; it is mitigated by pinning the contract in the fixture metadata.
</threat_model>

<verification>
- `cargo test -p sylva-core quantize::parity` (serde round-trip) and `cargo test -p sylva-core --test quantize_golden parity_golden` (contract) both green.
- `crates/sylva-core/tests/fixtures/quantize_golden_v1.json` committed and contains the `use_fast_math` contract note + a NaN column.
- `cargo fmt --check` and `cargo clippy -p sylva-core -- -D warnings` clean.
- No live GPU run is asserted (D-05 — contract only).
</verification>

<success_criteria>
- The QUANT-02 bit-parity contract is frozen as golden vectors and enforced by a passing CI test (SC-2 "as a contract").
- The fixture covers dense / low-cardinality / on-edge / NaN / out-of-range column kinds.
- The Phase-4 GPU compute requirement (pure compare, no FMA, no fast-math) is recorded in the fixture for forward carry.
</success_criteria>

<output>
Create `.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-02-SUMMARY.md` when done. State explicitly that SC-2/QUANT-02 is satisfied THIS phase as a contract (golden vectors) and that the live CPU↔GPU CI parity run is the Phase-4 deliverable validated against `quantize_golden_v1.json`.
</output>
