---
phase: 03-feature-quantizer-cpu-gpu-bit-parity
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/sylva-core/src/pyseam.rs
  - crates/sylva-core/src/lib.rs
  - python/tests/quantize_parity/__init__.py
  - python/tests/quantize_parity/conftest.py
  - python/tests/quantize_parity/datasets.py
  - python/tests/quantize_parity/test_numpy_oracle.py
  - python/tests/quantize_parity/test_sklearn_distributional.py
  - python/tests/quantize_parity/test_throughput.py
autonomous: true
requirements: [QUANT-01, QUANT-02]
user_setup:
  - service: maturin
    why: "Rebuild the pyseam wheel with the new quantize_cpu entry point so the Python harness can call the Rust Quantizer"
    dashboard_config:
      - task: "Run `maturin develop --features pyseam` from crates/sylva-core (into the pinned parity venv) after pyseam.rs changes"
        location: "crates/sylva-core (the pyseam pyproject.toml)"
must_haves:
  truths:
    - "A new quantize_cpu pyseam fn takes an f32 numpy array + n_bins and returns the column-major bins + a JSON QuantizeReport, mirroring fit_cpu/split_statistics"
    - "The numpy edge-exact harness asserts Sylva CPU bins == numpy.quantile(method='linear') + searchsorted(side='right') oracle at 100% bit-equality (the SC-4 GATE)"
    - "The sklearn _BinMapper harness reports bin-assignment agreement % as informational only (>=99% expected), never an edge-exact assertion (D-03/D-12)"
    - "A quantize-throughput microbench reports rows/s for Sylva-CPU vs numpy.quantile vs _BinMapper, cold/warm separated, repeated runs, pinned numpy 2.4.2 / sklearn 1.8.0 — op-level, NO end-to-end speed claim"
  artifacts:
    - path: "crates/sylva-core/src/pyseam.rs"
      provides: "quantize_cpu PyO3 fn + module registration"
      contains: "quantize_cpu"
    - path: "python/tests/quantize_parity/test_numpy_oracle.py"
      provides: "Edge-exact 100% bit-equality GATE vs numpy.quantile oracle"
      contains: "searchsorted"
    - path: "python/tests/quantize_parity/test_sklearn_distributional.py"
      provides: "Distributional (informational) agreement vs _BinMapper"
      contains: "_BinMapper"
    - path: "python/tests/quantize_parity/test_throughput.py"
      provides: "Op-level rows/s microbench, cold/warm separated, reported not gated"
      contains: "perf_counter"
    - path: "python/tests/quantize_parity/conftest.py"
      provides: "Version manifest (pinned numpy/sklearn) + dataset/fixture wiring"
      contains: "VERSION_MANIFEST"
  key_links:
    - from: "python/tests/quantize_parity/test_numpy_oracle.py"
      to: "crates/sylva-core/src/pyseam.rs"
      via: "imports sylva_core_pyseam and calls quantize_cpu"
      pattern: "quantize_cpu"
    - from: "crates/sylva-core/src/pyseam.rs"
      to: "crates/sylva-core/src/quantize/mod.rs"
      via: "quantize_cpu calls Quantizer::fit_transform"
      pattern: "Quantizer"
  prohibitions:
    - "MUST NOT assert Sylva edges equal sklearn _BinMapper thresholds — _BinMapper uses midpoints + opposite boundary side and is distributional-only by design (a 'edges match sklearn' test is a trap)"
    - "MUST NOT gate on throughput — SC-5 is reported, not gated (foundational-phase fairness, no end-to-end speed claim)"
    - "MUST NOT report 'data already on GPU' or any device-resident number — there is no device path this phase; the microbench is CPU op-level only"
    - "MUST NOT leave numpy/sklearn versions unpinned in the harness — record numpy 2.4.2 / sklearn 1.8.0 in the version manifest (fairness protocol)"
    - "MUST NOT panic or .unwrap() across the pyseam FFI boundary — map every fallible call to a typed Python exception (ValueError/RuntimeError)"
---

<objective>
Expose the Plan-01 `Quantizer` to Python via a new `quantize_cpu` pyseam fn and build the `python/tests/quantize_parity/` baseline harness: the numpy edge-exact GATE (SC-4), the sklearn `_BinMapper` distributional check (informational), and the quantize-throughput microbench (SC-5, reported not gated). This proves Sylva's binning is correct against the established baseline and records the op-level throughput honestly.

Purpose: Close QUANT-01's correctness-parity gate against `numpy.quantile` and deliver the Comparative Baseline Study (D-12). The vertical slice: a pinned numpy matrix in → 100% bit-equal bin agreement assertion + a reported rows/s number out.

Output: `quantize_cpu` in `pyseam.rs` (registered in the module), and a forked `python/tests/quantize_parity/` harness mirroring `python/tests/parity/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-CONTEXT.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-RESEARCH.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-PATTERNS.md
@.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-01-SUMMARY.md
@crates/sylva-core/src/pyseam.rs
@crates/sylva-core/src/quantize/mod.rs
@python/tests/parity/conftest.py
@python/tests/parity/datasets.py
@python/tests/parity/test_distributional_parity.py
</context>

<artifacts_this_phase_produces>
This plan creates (Phase-3 new symbols — excluded from any source-drift verification):
- `pyseam.rs`: new `#[pyfunction] #[pyo3(name = "quantize_cpu")] fn py_quantize_cpu(...)` returning `(bins_ndarray, report_json_string)`; registered in `sylva_core_pyseam` alongside `fit_cpu`/`predict_cpu`/`split_statistics`.
- Python package `python/tests/quantize_parity/` with: `__init__.py`, `conftest.py` (version manifest + 100k×100 f32 dataset fixtures, bins 128/256), `datasets.py` (`load_make_classification` 100k×100 f32 + NaN-injection helper), `test_numpy_oracle.py` (`numpy_oracle_bins` + 100% bit-equality GATE), `test_sklearn_distributional.py` (`_BinMapper` agreement %, informational), `test_throughput.py` (cold/warm rows/s microbench).

Note: `lib.rs` is touched only to (re-)confirm the `sylva_core_pyseam` registration compiles; no new top-level module. The numpy oracle `numpy_oracle_bins` is test-only (never a runtime dep).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: quantize_cpu pyseam fn + module registration (pyseam.rs)</name>
  <files>crates/sylva-core/src/pyseam.rs, crates/sylva-core/src/lib.rs (confirm pyseam registration compiles; no new module)</files>
  <read_first>
    - crates/sylva-core/src/pyseam.rs (lines 1-53 the FFI-safety doc + sylva_error_to_pyerr mapping; lines 228-270 the py_fit_cpu shape — array shape validation, py.detach GIL release, error mapping; lines 366-396 the module registration block to extend)
    - crates/sylva-core/src/quantize/mod.rs (Quantizer::fit_transform signature + return shape; QuantizeConfig; BinnedMatrix column-major accessor)
    - crates/sylva-core/src/quantize/report.rs (QuantizeReport — serialize to JSON for the Python side)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-PATTERNS.md (§Python harness "Sylva access" — the quantize_cpu seam mirrors fit_cpu/split_statistics)
  </read_first>
  <behavior>
    - `quantize_cpu(X: f32[n,d], n_bins: int) -> (bins: uint8[n,d] or uint16[n,d], report_json: str)`: validates X (non-empty, 2-D, f32) and n_bins range at entry → `ValueError` before work; releases the GIL for the quantize work; returns the column-major bins reshaped to (n,d) row-major numpy for the harness plus the `QuantizeReport` as a JSON string.
    - No `.unwrap()` / panic across FFI; `SylvaError` → typed `PyErr` via the existing `sylva_error_to_pyerr`.
    - For the default 256-bin (uint8) path the returned array dtype is uint8; the >256 path returns uint16.
  </behavior>
  <action>
    Edit `crates/sylva-core/src/pyseam.rs`: add `use crate::quantize::{Quantizer, QuantizeConfig};` and a `#[pyfunction] #[pyo3(name = "quantize_cpu")] fn py_quantize_cpu<'py>(py, x: PyReadonlyArray2<f32>, n_bins: usize) -> PyResult<(Bound<'py, PyArray2<...>>, String)>`. Mirror `py_fit_cpu` (lines 228-270): validate `x.shape()` (rows>0, cols>0) raising `PyValueError` early; build `QuantizeConfig` from `n_bins` (validate via `.validate().map_err(sylva_error_to_pyerr)`); `py.detach(|| Quantizer::fit_transform(x_view))`; map `SylvaError` via `sylva_error_to_pyerr`. Convert the column-major `BinnedMatrix` to a row-major `(n,d)` numpy array (uint8 or uint16 per `BinDtype`) via `IntoPyArray`; serialize `QuantizeReport` with `serde_json::to_string` (map error to `PyRuntimeError`). Register it in `sylva_core_pyseam` (lines 390-396): `module.add_function(wrap_pyfunction!(py_quantize_cpu, module)?)?;`. Update the module doc (lines 1-28) to list `quantize_cpu` among the exposed fns (still strictly below EST-02). Do NOT add a separate top-level module to `lib.rs`; the `#[cfg(feature = "pyseam")] pub mod pyseam;` line already exists — confirm it still compiles.
  </action>
  <verify>
    <automated>cargo build -p sylva-core --features pyseam</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p sylva-core --features pyseam` succeeds with `quantize_cpu` registered.
    - `cargo clippy -p sylva-core --features pyseam -- -D warnings` is clean (no `.unwrap()` across FFI).
    - The default build (no `pyseam` feature) remains pyo3-free: `cargo build -p sylva-core` succeeds.
  </acceptance_criteria>
  <done>quantize_cpu crosses the PyO3 boundary returning column-major bins + a JSON QuantizeReport, mirrors the fit_cpu seam discipline (early validation, GIL release, typed errors), and is registered in sylva_core_pyseam.</done>
</task>

<task type="auto">
  <name>Task 2: quantize_parity harness scaffold + numpy edge-exact GATE (conftest, datasets, test_numpy_oracle)</name>
  <files>python/tests/quantize_parity/__init__.py, python/tests/quantize_parity/conftest.py, python/tests/quantize_parity/datasets.py, python/tests/quantize_parity/test_numpy_oracle.py</files>
  <read_first>
    - python/tests/parity/conftest.py (lines 1-71 the VERSION_MANIFEST + autouse print; copy and adapt for numpy/sklearn pinning)
    - python/tests/parity/datasets.py (lines 1-90 the `load_make_classification` + `Dataset` NamedTuple + fixed-seed constants to fork)
    - python/tests/parity/test_distributional_parity.py (lines 1-25 the fairness-protocol module docstring shape; lines 41,123 the `import sylva_core_pyseam as sylva` + seam-call pattern)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-RESEARCH.md (§Code Examples "Python baseline oracle"; §Validation Architecture Tolerances; Pitfall 1; Pitfall 4 NaN)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-PATTERNS.md (§Python harness assignment — exact-match fork)
  </read_first>
  <behavior>
    - `numpy_oracle_bins(X, n_data_bins, missing_idx)` implements RESEARCH Code Examples exactly: `q = linspace(0,1,n_data_bins+1)[1:-1]`, per-feature `edges = np.unique(np.quantile(finite, q, method='linear'))`, `b = np.searchsorted(edges, col, side='right')`, `b[isnan] = missing_idx`.
    - The GATE test asserts `sylva_bins == numpy_oracle_bins` at 100% bit-equality (`np.array_equal`) on `make_classification` 100k×100 f32 with NaN injected into some cells, for bins 128 and 256.
    - Version manifest records pinned numpy 2.4.2 / sklearn 1.8.0 and the sylva commit.
  </behavior>
  <action>
    Create `python/tests/quantize_parity/__init__.py` (empty package marker). Create `conftest.py` forking `python/tests/parity/conftest.py:1-71`: copy `VERSION_MANIFEST` (python/sklearn/numpy/sylva_commit/dataset_seed) + the autouse `print_version_manifest` fixture; assert/record numpy 2.4.2 and sklearn 1.8.0. Create `datasets.py` forking `python/tests/parity/datasets.py`: `load_quantize_dataset()` building `make_classification(n_samples=100_000, n_features=100, random_state=DATASET_SEED).astype(np.float32)` plus a `inject_nans(X, frac, seed)` helper (deterministic NaN injection for the missing-bin path); expose `N_BINS_VARIANTS = (128, 256)` and a `MISSING_IDX_FOR(n_bins)` helper (= n_bins). Create `test_numpy_oracle.py`: open with the `[FP-1..6]`-style fairness docstring framing "numpy = edge-exact 100% GATE; sklearn = distributional informational" (RESEARCH Tolerances); implement `numpy_oracle_bins` per RESEARCH Code Examples; `import sylva_core_pyseam as sylva`; for each n_bins in (128,256) call `sylva.quantize_cpu(X, n_bins)`, compute the oracle, and `assert np.array_equal(sylva_bins, oracle_bins)` (the SC-4 GATE). Include the NaN-injected variant asserting NaN cells == missing_idx in both.
  </action>
  <verify>
    <automated>cd python && python -m pytest tests/quantize_parity/test_numpy_oracle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/quantize_parity/test_numpy_oracle.py` passes: 100% bit-equality (`np.array_equal`) vs the numpy oracle for bins 128 and 256.
    - The NaN-injected variant asserts NaN cells land in `missing_idx` (= n_bins) in both Sylva and oracle.
    - The version manifest prints pinned numpy 2.4.2 / sklearn 1.8.0 at session start.
    - (Requires `maturin develop --features pyseam` first — see user_setup.)
  </acceptance_criteria>
  <done>The numpy edge-exact GATE passes at 100% bit-equality on the pinned 100k×100 f32 dataset for bins 128/256 incl. NaN routing — Sylva's binning is provably correct against numpy.quantile.</done>
</task>

<task type="auto">
  <name>Task 3: sklearn distributional check (informational) + throughput microbench (reported, not gated)</name>
  <files>python/tests/quantize_parity/test_sklearn_distributional.py, python/tests/quantize_parity/test_throughput.py</files>
  <read_first>
    - python/tests/parity/test_distributional_parity.py (lines 1-25 fairness docstring; lines 262-319 the cold/warm two-fit perf_counter microbench shape to copy)
    - python/tests/quantize_parity/datasets.py (the forked loader from Task 2 — reuse)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-RESEARCH.md (Pitfall 3 _BinMapper is NOT edge-exact; §Validation Architecture Tolerances — sklearn >=99% informational, throughput reported-not-gated, cold/warm separated)
    - .planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-VALIDATION.md (§Manual-Only Verifications — the sklearn threshold + throughput are informational reads, not hard gates)
  </read_first>
  <behavior>
    - sklearn check: fit `sklearn ..._hist_gradient_boosting.binning._BinMapper` (or the public HGBT binning path) on the same X, map bins, and REPORT bin-assignment agreement % vs Sylva — informational, expected >=99%, with <95% flagged as "investigate" not auto-fail. NO assertion that edges equal `_BinMapper` thresholds.
    - throughput: time `sylva.quantize_cpu`, `numpy.quantile`-based binning, and `_BinMapper` binning on the pinned dataset; cold run then warm runs (repeated); report rows/s for each; print a results table; assert NOTHING about relative speed (reported, not gated).
  </behavior>
  <action>
    Create `python/tests/quantize_parity/test_sklearn_distributional.py`: open with the fairness docstring explicitly stating `_BinMapper` is a DISTRIBUTIONAL baseline (midpoints + opposite boundary side), NOT edge-exact (RESEARCH Pitfall 3). Import `_BinMapper` from `sklearn.ensemble._hist_gradient_boosting.binning` (pinned 1.8.0); fit on X, transform to bins, compute `agreement = (sylva_bins == binmapper_bins).mean()`, and `print` it with the n_bins variant; assert only a loose informational floor (e.g. `>= 0.90`) documented as a divergence-aware sanity check, NOT an edge-equality assertion. Record the observed agreement % in the test output for the study report.
    Create `python/tests/quantize_parity/test_throughput.py`: copy the `test_distributional_parity.py:262-319` cold/warm `time.perf_counter()` structure; measure rows/s for Sylva-CPU (`sylva.quantize_cpu`), `numpy.quantile`-based binning, and `_BinMapper` on `make_classification` 100k×100 f32, bins 128 and 256, repeated runs, cold and warm SEPARATED; `print` a rows/s table with the version manifest; mark the test `@pytest.mark.benchmark`-style informational and assert nothing about relative throughput (foundational-phase fairness — op-level, NO end-to-end speed claim, NO device-resident number). Add a module docstring stating SC-5 is reported-not-gated.
  </action>
  <verify>
    <automated>cd python && python -m pytest tests/quantize_parity/test_sklearn_distributional.py tests/quantize_parity/test_throughput.py -q -s</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/quantize_parity/test_sklearn_distributional.py tests/quantize_parity/test_throughput.py -s` passes; the sklearn agreement % and the rows/s table are printed.
    - `test_sklearn_distributional.py` contains NO assertion that Sylva edges equal `_BinMapper` thresholds (grep confirms only agreement-% / distributional assertions).
    - `test_throughput.py` asserts nothing about relative speed and prints cold and warm rows/s separately for all three implementations with pinned versions.
  </acceptance_criteria>
  <done>The sklearn _BinMapper distributional agreement is reported (informational, never edge-exact) and the op-level quantize-throughput microbench reports cold/warm rows/s for Sylva-CPU vs numpy vs _BinMapper with no end-to-end speed claim — the D-12 Comparative Baseline Study is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python harness → `quantize_cpu` FFI | Untrusted numpy array crosses the PyO3 boundary into the Rust core; shape/dtype/n_bins validated at entry → typed Python exception. |
| test-only numpy/sklearn oracles | numpy/sklearn are test-only oracles, never shipped runtime deps — no contamination of the crate's dependency graph (pyseam is a Cargo feature, off by default). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / DoS | `py_quantize_cpu` (malformed numpy array / bad n_bins crossing FFI) | mitigate | Validate `x.shape()` (non-empty 2-D) + dtype (f32 enforced by `PyReadonlyArray2<f32>`) + n_bins range at entry → `ValueError` before any work; no `.unwrap()`/panic across FFI (mirrors `py_fit_cpu` T-02-16 control). (high — the only untrusted-input FFI surface.) |
| T-03-08 | Information disclosure (dishonest benchmark) | `test_throughput.py` | mitigate | Op-level rows/s only, cold/warm separated, no device-resident/"already on GPU" number, pinned versions recorded — foundational-phase fairness (PITFALLS 1/2/13); reported, never gated. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new shipped packages; numpy 2.4.2 / sklearn 1.8.0 are pre-installed test-only oracles (RESEARCH §Package Legitimacy Audit: zero new packages, all OK). No `[ASSUMED]`/`[SUS]` → no legitimacy checkpoint required. |

Block-on severity: high. T-03-07 (FFI input validation) is the high-severity item, mitigated by boundary validation mirroring the established `py_fit_cpu` control.
</threat_model>

<verification>
- `cargo build -p sylva-core --features pyseam` green; default `cargo build -p sylva-core` stays pyo3-free.
- After `maturin develop --features pyseam`: `python -m pytest tests/quantize_parity -q` — numpy oracle GATE passes at 100% bit-equality; sklearn distributional + throughput run and print informational numbers.
- `test_sklearn_distributional.py` has no edge-equality-vs-sklearn assertion; `test_throughput.py` gates nothing on speed.
- Version manifest records pinned numpy 2.4.2 / sklearn 1.8.0.
</verification>

<success_criteria>
- QUANT-01 correctness parity: Sylva CPU bins == numpy.quantile oracle at 100% bit-equality on the pinned 100k×100 f32 dataset, bins 128/256 (SC-4 GATE).
- D-12 Comparative Baseline Study delivered: sklearn distributional agreement reported (informational) + op-level quantize-throughput microbench reported (SC-5, not gated, no end-to-end speed claim).
- `quantize_cpu` pyseam fn lands without breaking the default pyo3-free build.
</success_criteria>

<output>
Create `.planning/phases/03-feature-quantizer-cpu-gpu-bit-parity/03-03-SUMMARY.md` when done. Record the observed numpy bit-equality (must be 100%), the observed sklearn `_BinMapper` agreement % (informational), and the cold/warm rows/s table for the Comparative Baseline Study; note GPU quantize throughput is the Phase-4 deliverable (D-12).
</output>
