---
phase: 07-crossover-benchmark-gate-3
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - python/benchmarks/crossover_study.py
  - python/tests/test_crossover_accuracy_parity.py
autonomous: false
requirements: [BENCH-01, BENCH-02]
user_setup:
  - service: sklearnex
    why: "Strongest CPU baseline for the crossover study (per D-2/D-6 fairness protocol)"
    env_vars: []
    dashboard_config:
      - task: "Install scikit-learn-intelex in the benchmark venv and verify ExtraTrees is among the patched estimators"
        location: "pip install scikit-learn-intelex; python -c \"from sklearnex import get_patch_map; print(get_patch_map().keys())\""
  - service: cuml
    why: "GPU RF baseline for the RF-vs-RF comparison group (labeled as a different algorithm from ET)"
    env_vars: []
    dashboard_config:
      - task: "Attempt cuML install on Windows or via WSL2; record availability and version in crossover_manifest.py MANIFEST"
        location: "pip install cuml-cu12 (Linux/WSL2) or document 'not available on measurement host'"
must_haves:
  truths:
    - "crossover_study.py timed_fit starts time.perf_counter() BEFORE X_f64.astype(np.float32) and ends it AFTER model.fit(X32, y) returns — dtype coercion + H2D + quantization are ALL inside the timed region with no exceptions"
    - "crossover_study.py run_crossover_study loops over the full SYNTHETIC_GRID (24 cells) plus real anchors; every cell is wrapped in safe_timed_fit so OOM does not crash the loop; oom=True cells remain in the results JSON"
    - "The BenchmarkImpl.algorithm tag is 'et' or 'rf' for every impl; the crossover surface only computes speedup ratios within the same algorithm group — no ET-vs-RF comparison is possible structurally"
    - "sklearnex and cuML availability are resolved at a blocking human-verify checkpoint before the study harness runs; the MANIFEST is updated with actual versions or 'not available on measurement host'"
    - "test_crossover_accuracy_parity.py asserts Sylva ET/RF accuracy is within 1% of sklearn ET/RF on a CI-sized fixed-seed dataset with device='cpu' — the gate; no speed assertion"
    - "crossover_study.py --smoke flag runs exactly one (n=10_000, d=20) cell per implementation to prove the harness end-to-end without the full measurement cost"
  artifacts:
    - path: "python/benchmarks/crossover_study.py"
      provides: "(n×d) grid loop harness with end-to-end timing, OOM-safe, like-for-like enforcement, accuracy beside speed, JSON/CSV output"
      contains: "timed_fit"
    - path: "python/tests/test_crossover_accuracy_parity.py"
      provides: "accuracy-parity GATE (CI-portable, device='cpu', ET-vs-ET, RF-vs-RF)"
      contains: "ACCURACY_TOLERANCE"
  key_links:
    - from: "python/benchmarks/crossover_study.py"
      to: "python/benchmarks/grid_spec.py"
      via: "imports SYNTHETIC_GRID, REAL_ANCHORS, ET_IMPLS, RF_IMPLS, BenchmarkImpl, make_grid_dataset, make_covertype_dataset"
      pattern: "from .grid_spec"
    - from: "python/benchmarks/crossover_study.py"
      to: "python/benchmarks/crossover_manifest.py"
      via: "imports MANIFEST; writes MANIFEST into the results JSON header"
      pattern: "from .crossover_manifest"
    - from: "python/tests/test_crossover_accuracy_parity.py"
      to: "python/tests/parity/datasets.py"
      via: "reuses CLF_HYPERPARAMS, RF_CLF_HYPERPARAMS, DATASET_SEED; CI-sized fixture"
      pattern: "from .parity.datasets"
---

<objective>
Build the (n×d) crossover study harness and verify accuracy parity.

Plan 02 delivers the measurement engine: crossover_study.py with the end-to-end timing loop
(timed_fit starting BEFORE dtype coercion, per D-2 LOCKED), OOM-safe grid runner, like-for-like
enforcement, JSON/CSV output, and a --smoke mode for quick end-to-end verification. It also
delivers the CI-portable accuracy-parity gate (test_crossover_accuracy_parity.py) that confirms
Sylva ET/RF accuracy is within 1% of sklearn ET/RF on a fixed-seed dataset — this is BENCH-02's
accuracy gate, independent of the GPU speed measurement.

A blocking human-verify checkpoint resolves sklearnex and cuML availability before the harness
is considered ready to run. The checkpoint records actual versions or honest "not available" into
the MANIFEST and determines whether the strongest CPU baseline includes oneDAL acceleration.

Purpose: After this plan a developer can: (a) run the smoke test to prove the harness end-to-end,
(b) confirm accuracy parity in CI, (c) record baseline availability on the measurement host.
The full measurement run (Plan 03) is then unblocked.

Output:
  - crossover_study.py (harness), updated crossover_manifest.py (baseline versions filled in)
  - test_crossover_accuracy_parity.py (accuracy gate, CI-portable)

Decisions implemented: D-2 (timing contract — coercion + H2D inside timed region), D-3
(deterministic=False fast path as primary speed claim), D-4 (kill criterion at human-verify in
Plan 03 — this plan builds the engine; Plan 03 pulls the trigger), D-5 (Phase-5 files untouched),
D-6 (fairness rules as structural gates in the harness).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md
@.planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md
@.planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md
@python/benchmarks/grid_spec.py
@python/benchmarks/crossover_manifest.py
@python/tests/parity/conftest.py
@python/tests/parity/datasets.py
@python/tests/parity/test_distributional_parity.py
</context>

## Assumptions

- D-2 LOCKED: timing contract — perf_counter() starts before X_f64.astype(np.float32) every call. Alternative (CUDA events for device-only time) is explicitly rejected per RESEARCH.md §Alternatives Considered and PITFALL 1.
- D-3 LOCKED: primary speed cells use deterministic=False (the fast path). If Phase 6 ships without a deterministic parameter, use default params and document. deterministic=True results reported in a separate column using the Phase-6 measured overhead fraction.
- D-5 LOCKED: comparative_study.py and study_manifest.py from Phase 5 are not imported or modified. crossover_study.py imports from grid_spec.py and crossover_manifest.py only.
- sklearnex human-verify checkpoint must determine whether ExtraTreesClassifier is among the patched estimators. If ET is not patched by sklearnex on this host, sklearn n_jobs=-1 is the only ET CPU baseline; record this in the MANIFEST as "sklearnex_et_patched: false".
- Alternative for Sylva import name: if the package is not yet importable as "sylva" (Phases 3-6 pending), the crossover_study.py stub imports with a try/except and marks GPU cells as "sylva_not_available" until Phase 6 ships. The harness must not fail to parse/import in the absence of the Sylva wheel.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Resolve sklearnex and cuML baseline availability on the measurement host (D-6)</name>
  <files>python/benchmarks/crossover_manifest.py</files>
  <what-built>crossover_manifest.py MANIFEST with placeholder baseline version fields ("not yet resolved — see checkpoint:human-verify") for sklearnex_version and cuml_version. The harness scaffold (crossover_study.py) is partially built with conditional import guards for sklearnex and cuML.</what-built>
  <action>After the human verify step completes, update crossover_manifest.py MANIFEST to replace the placeholder values for sklearnex_version, sklearnex_et_patched, cuml_version, and cuml_path with the actual resolved values from the verify step. If sklearnex is not available, set sklearnex_version to "not available on measurement host". If cuML is not available, set cuml_version to "not available on measurement host". Record which baseline is the strongest CPU ET baseline (sklearn n_jobs=-1 only, or sklearn + sklearnex ET).</action>
  <verify><automated>python -c "from benchmarks.crossover_manifest import MANIFEST; assert MANIFEST.get('sklearnex_version') not in ('not yet resolved — see checkpoint:human-verify', None), 'checkpoint not resolved'; print('MANIFEST baseline fields resolved')"</automated></verify>
  <done>MANIFEST["sklearnex_version"] and MANIFEST["cuml_version"] contain actual resolved values (version strings or "not available on measurement host") — not the placeholder string. The strongest CPU ET baseline is documented in the MANIFEST.</done>
  <how-to-verify>
    On the Windows measurement host (RTX 4060 Ti, CUDA 12.8, driver 595.79):

    1. Install and verify sklearnex:
       pip install scikit-learn-intelex
       python -c "from sklearnex import patch_sklearn, get_patch_map; patch_sklearn(); print(list(get_patch_map().keys()))"
       Confirm: (a) import succeeds, (b) 'ExtraTrees' appears in get_patch_map() output.
       If ExtraTrees is NOT in the patch map, sklearnex does NOT accelerate ET on this host — record
       sklearnex_et_patched: false; the strongest ET CPU baseline is sklearn n_jobs=-1 only.

    2. Install and verify cuML:
       Try: pip install cuml-cu12 (on native Windows — expected to fail).
       If it fails: attempt WSL2 path with GPU passthrough, or document "cuML: not available on
       measurement host — Windows install unsupported; WSL2 path not configured."
       Record whichever outcome is true; do NOT fake a cuML result.

    3. Record resolved versions into crossover_manifest.py MANIFEST:
       MANIFEST["sklearnex_version"] = the installed version string or "not available"
       MANIFEST["sklearnex_et_patched"] = True or False
       MANIFEST["cuml_version"] = the version string or "not available on measurement host"
       MANIFEST["cuml_path"] = "native" | "wsl2" | "not available"

    4. Record the chosen strongest CPU ET baseline: if sklearnex patches ET, it is the strongest
       CPU ET baseline. If not, sklearn n_jobs=-1 is the strongest. This determines the denominator
       in the win condition: best_cpu_et_warm_median = min of available ET baselines.
  </how-to-verify>
  <resume-signal>Reply with: sklearnex version (or "not available"), whether ET is in the patch map (yes/no), cuML version (or "not available on host"), and the chosen strongest CPU ET baseline. Update crossover_manifest.py MANIFEST with these values before resuming. Do NOT auto-approve — this checkpoint requires a human to run the install commands on the measurement host.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: crossover_study.py — (n×d) grid harness with end-to-end timing, OOM-safe, like-for-like</name>
  <files>python/benchmarks/crossover_study.py</files>
  <read_first>
    - .planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md — crossover_study.py imports pattern, BenchmarkImpl pattern, timed_fit pattern (canonical timing contract from RESEARCH.md Pattern 1), safe_timed_fit pattern (RESEARCH.md Pattern 3), grid loop pattern (RESEARCH.md architecture diagram), sklearnex patching pattern (RESEARCH.md Pattern 5)
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Pattern 1 (timing contract comments — anti-pattern description: starting timer after dtype coercion), Pattern 2 (BenchmarkImpl.algorithm tag, ET/RF structural grouping), Pattern 3 (OOM-safe wrapper), Pattern 5 (sklearnex patch_sklearn call sequence), Anti-Patterns section
    - python/benchmarks/grid_spec.py — SYNTHETIC_GRID, ET_IMPLS, RF_IMPLS, BenchmarkImpl, make_grid_dataset, make_covertype_dataset (already written in Plan 01 Task 2)
    - python/benchmarks/crossover_manifest.py — MANIFEST dict (to write into results JSON header)
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Architecture Patterns §Recommended Project Structure (output files: results/crossover_results.json, crossover_results.csv)
  </read_first>
  <behavior>
    - timed_fit correctness: given a model factory, X_f64 (float64 numpy array), y; must return a dict with cold_times (list), warm_times (list), median_cold_s, median_warm_s, iqr_warm_s; the dtype coercion X_f64.astype(np.float32) is the FIRST statement after t0 = time.perf_counter() — the timer must be started before the coercion, not after.
    - safe_timed_fit correctness: given a BenchmarkImpl and X_f64, y; must return {"oom": False/True, "error": None/str, "accuracy": float/None, "median_cold_s": float/None, "median_warm_s": float/None, ...}; catches MemoryError and RuntimeError containing "out of memory"; accuracy is computed on a test split using the fitted model.
    - algorithm grouping: the surface loop must NEVER compare an ET impl's result to an RF impl's result in a speedup computation; assert impl.algorithm in ("et", "rf") before every cell.
    - smoke mode: when invoked with --smoke (or a SMOKE=True env var), runs exactly one grid cell (n=10_000, d=20) per implementation and exits after writing the output JSON. The smoke run proves the harness end-to-end without the full cost.
    - output: writes results/crossover_results.json (list of per-cell dicts, each containing n, d, impl, algorithm, dataset, median_cold_s, median_warm_s, iqr_warm_s, accuracy, oom, error) and results/crossover_results.csv (same data, tabular). MANIFEST is written as a header field in the JSON.
  </behavior>
  <action>
    Create python/benchmarks/crossover_study.py with the following structure:

    Module docstring: include FP-1..FP-6 fairness protocol tag block (mirror test_distributional_parity.py
    header style). FP-1: ET-vs-ET, RF-vs-RF only (PITFALL 13). FP-2: timed region includes dtype
    coercion + H2D + quantization (PITFALL 1). FP-3: cold and warm runs separated. FP-4: accuracy
    beside every speed cell. FP-5: OOM/failures reported honestly. FP-6: n_jobs=-1 for all sklearn
    baselines.

    Imports: from __future__ import annotations; json, statistics, time, csv, sys, argparse,
    pathlib.Path, dataclasses.dataclass, typing.Any; numpy as np; from .grid_spec import ...; from
    .crossover_manifest import MANIFEST.

    sklearnex conditional patch (per RESEARCH.md Pattern 5): try/except ImportError block that calls
    patch_sklearn() BEFORE importing sklearn estimators. Set SKLEARNEX_AVAILABLE = True/False. Add
    this call at module import time so all subsequently-imported sklearn estimators are patched if
    available. Record result in a module-level variable consumed by the impl list.

    Sylva conditional import: try/except ImportError for sylva_core_pyseam (or the correct wheel
    import name after Phase 5/6). Set SYLVA_AVAILABLE = True/False. If False, GPU impls emit
    oom=False, error="sylva not available — run after Phase 6 ships" and are excluded from the
    crossover computation.

    timed_fit(model, X_f64, y, n_cold=1, n_warm=5) function: the canonical timing implementation
    per RESEARCH.md Pattern 1. CRITICAL: t0 = time.perf_counter() is placed BEFORE X32 =
    X_f64.astype(np.float32). This is the structural enforcement of D-2. Every call (cold and
    warm) follows the same pattern. Returns dict with cold_times, warm_times, median_cold_s,
    median_warm_s, iqr_warm_s (IQR computed over warm_times using sorted indexing).

    safe_timed_fit(impl: BenchmarkImpl, X_f64, y, X_test_f64, y_test, **kwargs): wraps timed_fit
    in try/except; catches MemoryError and RuntimeError; on success also calls model.predict
    (after coercion of X_test_f64) and records accuracy_score; returns the full result dict.
    ALWAYS records accuracy beside timing (FP-4 gate).

    ET and RF impl construction function (or module-level list): builds the impl list by extending
    ET_IMPLS and RF_IMPLS from grid_spec with Sylva GPU entries (if SYLVA_AVAILABLE). For Sylva
    GPU ET: BenchmarkImpl("sylva_gpu_et", "et", "gpu", False, lambda: sylva.ExtraTreesClassifier(
    device='cuda', deterministic=False, **{k: v for k, v in CLF_HYPERPARAMS.items()})). For Sylva
    GPU RF: BenchmarkImpl("sylva_gpu_rf", "rf", "gpu", False, lambda: sylva.RandomForestClassifier(
    device='cuda', deterministic=False, **{k: v for k, v in RF_CLF_HYPERPARAMS.items()})). For
    Sylva CPU variants: BenchmarkImpl("sylva_cpu_et", "et", "cpu", False, lambda:
    sylva.ExtraTreesClassifier(device='cpu', **{...})). cuML RF if available: BenchmarkImpl(
    "cuml_rf_DIFFERENT_ALGO", "rf", "cpu", False, lambda: cuml.ensemble.RandomForestClassifier(**
    {k: v for k, v in RF_CLF_HYPERPARAMS.items() if k != 'n_jobs'})) — NOTE: algorithm tag is "rf",
    cuML is NOT in the ET group (asserted in test_crossover_fairness_rules.py).

    run_crossover_study(output_dir=Path("results"), smoke=False): creates output_dir; initializes
    results list; iterates SYNTHETIC_GRID cells (or one smoke cell); for each (n, d) calls
    make_grid_dataset, then for each impl in all_impls calls safe_timed_fit; asserts impl.algorithm
    in ("et", "rf") before appending; appends the row dict; after the synthetic grid, runs the
    Covertype anchor via make_covertype_dataset if not smoke; writes crossover_results.json (with
    MANIFEST as a top-level "manifest" key alongside "results" key) and crossover_results.csv.

    main(): argparse with --smoke flag; calls run_crossover_study(smoke=args.smoke).

    if __name__ == "__main__": main().
  </action>
  <verify>
    <automated>cd python && python -c "from benchmarks.crossover_study import timed_fit, safe_timed_fit, run_crossover_study; print('imports OK')" && python benchmarks/crossover_study.py --smoke 2>&1 | tail -5 && test -f results/crossover_results.json && echo "smoke run wrote JSON"</automated>
  </verify>
  <done>crossover_study.py imports cleanly; --smoke mode runs exactly one cell per impl and writes results/crossover_results.json; the timed_fit function's timer starts before dtype coercion; no ET-vs-RF comparison exists in the results JSON from the smoke run; accuracy is present in every result row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_crossover_accuracy_parity.py — accuracy-parity GATE (CI-portable, device='cpu')</name>
  <files>python/tests/test_crossover_accuracy_parity.py</files>
  <read_first>
    - .planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md — test_crossover_accuracy_parity.py pattern (imports, ACCURACY_TOLERANCE, FIXED_SEED, test body structure, [FP-1]/[FP-3] comments)
    - python/tests/parity/test_distributional_parity.py — test body layout, fixture usage, tolerance assertion message format to mirror
    - python/tests/parity/datasets.py — CLF_HYPERPARAMS, RF_CLF_HYPERPARAMS, DATASET_SEED, existing dataset fixtures (session-scoped) that can be reused
    - python/tests/parity/conftest.py — fixture definitions (clf_dataset, reg_dataset) — use these rather than re-creating dataset loading code
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Phase Requirements BENCH-02 (accuracy parity reported alongside speed, this test is the accuracy gate), Kill Criterion (ACCURACY_THRESHOLD = 0.01, 1%)
  </read_first>
  <behavior>
    - test_et_clf_accuracy_parity: Sylva ExtraTreesClassifier(device='cpu', **CLF_HYPERPARAMS, random_state=FIXED_SEED) vs sklearn ExtraTreesClassifier(**CLF_HYPERPARAMS, n_jobs=-1, random_state=FIXED_SEED). |sylva_acc - sklearn_acc| <= ACCURACY_TOLERANCE (0.01). Like-for-like: ET-vs-ET. No speed assertion.
    - test_rf_clf_accuracy_parity: same for RandomForestClassifier using RF_CLF_HYPERPARAMS.
    - test_et_reg_accuracy_parity: ExtraTreesRegressor, R2 score within tolerance.
    - test_rf_reg_accuracy_parity: RandomForestRegressor, R2 score within tolerance.
    - All four tests run with device='cpu' so no GPU is required for CI.
  </behavior>
  <action>
    Create python/tests/test_crossover_accuracy_parity.py:

    Module docstring: "Phase-7 crossover accuracy-parity GATE (BENCH-02). Asserts Sylva ET/RF test-
    set accuracy is within ACCURACY_TOLERANCE of sklearn ET/RF like-for-like (ET-vs-ET, RF-vs-RF).
    Runs with device='cpu' so it is CI-portable. Speed is NOT asserted here — speed is measured by
    crossover_study.py and reported, not gated. This is the BENCH-02 accuracy gate per the
    07-PRE-REGISTRATION.md kill criterion. Fairness tags: [FP-1] ET-vs-ET, RF-vs-RF only.
    [FP-3] Accuracy is the gate; speed is reported."

    Constants: ACCURACY_TOLERANCE: float = 0.01 (comment: "from 07-PRE-REGISTRATION.md kill
    criterion; 1% accuracy delta is the threshold for 'parity'; any gap larger than this signals
    an algorithmic divergence bug, not measurement noise"). R2_TOLERANCE: float = 0.03 (comment:
    "R2 tolerance is slightly wider than accuracy to account for regressor variance on small CI
    datasets; calibrated against Phase-2 parity tests"). FIXED_SEED: int = 42.

    Fixtures: import clf_dataset and reg_dataset from conftest.py (scope="session"); these return
    the standard CI-sized dataset (not the large grid datasets). If these fixtures are not available
    in the Phase-7 conftest scope, call make_classification directly with n_samples=5_000,
    n_features=20, random_state=FIXED_SEED and train_test_split.

    Four test functions following the PATTERNS.md layout: train both Sylva (device='cpu') and
    sklearn with IDENTICAL hyperparameters + same random_state=FIXED_SEED; compute test-set
    accuracy or R2; assert |delta| <= tolerance with a message that includes the actual values,
    the tolerance, and the text "BENCH-02 accuracy-parity gate failed".

    PROHIBITION: no timing assertion in any test function. No `time.perf_counter()` calls. No
    assertion on fit duration.
  </action>
  <verify>
    <automated>cd python && python -m pytest tests/test_crossover_accuracy_parity.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>All four accuracy-parity tests pass (ET clf, RF clf, ET reg, RF reg) with device='cpu', CI-portable. No speed assertion exists in the file. The module docstring includes FP-1 and FP-3 tags. ACCURACY_TOLERANCE is 0.01 with documented justification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python API call → timed_fit | Timer must encompass the full user-facing cost; starting it after coercion is the benchmark-dishonesty boundary (PITFALL 1) |
| harness impl list → ET/RF groups | Algorithm tag on BenchmarkImpl prevents ET-vs-RF cross-comparison at the code level, not by convention |
| sklearnex/cuML install → MANIFEST | Unverified packages are ASSUMED; human-verify checkpoint is the enforcement boundary for these [ASSUMED] packages (per Package Legitimacy Audit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Repudiation / Tampering | timed_fit starts timer after dtype coercion | mitigate | Code structure: t0 = time.perf_counter() is the FIRST statement before X32 = X_f64.astype; code review during executor pass must verify ordering; RESEARCH.md Pattern 1 is the normative reference |
| T-07-06 | Repudiation | ET speedup computed against RF baseline | mitigate | impl.algorithm assertion before every cell; ET_IMPLS and RF_IMPLS are disjoint (asserted by fairness tests from Plan 01); speedup computation gated on same algorithm group |
| T-07-07 | Tampering | OOM cells silently dropped from the results JSON | mitigate | safe_timed_fit always appends its return value (oom=True row) before continuing; the JSON contains every cell including OOM |
| T-07-08 | Repudiation | speed claim without accuracy beside it | mitigate | safe_timed_fit always calls accuracy_score and records the result; accuracy=None only for OOM/error cells, which cannot claim a speed win |
| T-07-09 | Tampering | sklearnex or cuML installed without human verification | mitigate | blocking human-verify checkpoint resolves availability before the harness is used; packages are benchmark-env-only, not Sylva runtime deps |
| T-07-SC | Tampering | [ASSUMED] sklearnex + cuml installs | mitigate | blocking human-verify checkpoint gates the install; slopcheck required: verify npmjs/pypi source before install (scikit-learn-intelex: intel.com / PyPI; cuml-cu12: rapids.ai / conda-forge); [ASSUMED] packages must not be auto-approved |
</threat_model>

<verification>
- crossover_study.py imports cleanly with no GPU.
- --smoke flag runs exactly one cell per implementation (n=10_000, d=20) and produces results/crossover_results.json with every impl's row including oom and accuracy fields.
- The results JSON from the smoke run contains NO row where algorithm_comparison crosses ET to RF.
- timed_fit source: t0 = time.perf_counter() appears on the line immediately before X32 = X_f64.astype(np.float32) in both cold and warm loops.
- pytest tests/test_crossover_accuracy_parity.py passes green with no GPU (device='cpu').
- The baseline-availability checkpoint is recorded as a blocking-human gate (not auto-approved).
- crossover_manifest.py MANIFEST has sklearnex_version and cuml_version fields filled in (not placeholder strings) after the checkpoint.
</verification>

<success_criteria>
- A developer can run python benchmarks/crossover_study.py --smoke to verify the harness end-to-end in under 2 minutes.
- pytest tests/test_crossover_accuracy_parity.py is green in CI (GPU-less).
- The harness is structurally incapable of producing an ET-vs-RF speedup comparison.
- The baseline availability (sklearnex, cuML) is honestly recorded in the MANIFEST.
- The timing region is correct: every timed_fit call includes dtype coercion + H2D + quantization.
</success_criteria>

<output>
Create `.planning/phases/07-crossover-benchmark-gate-3/07-02-SUMMARY.md` when done.
</output>
