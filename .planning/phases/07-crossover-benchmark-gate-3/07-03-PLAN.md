---
phase: 07-crossover-benchmark-gate-3
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - python/benchmarks/visualize_surface.py
  - results/crossover_results.json
  - results/crossover_results.csv
  - results/crossover_table.md
  - results/crossover_surface.png
autonomous: false
requirements: [BENCH-01, BENCH-02, BENCH-03]
must_haves:
  truths:
    - "The full (n×d) grid (24 synthetic cells + 2 real anchors) has been measured on the dev host and results/crossover_results.json contains a row for every pre-registered cell including OOM cells — no cell is silently absent"
    - "Every row in the results JSON has both a median_warm_s (or oom=True) AND an accuracy (or accuracy=None for OOM/error) field — speed is never reported without accuracy alongside"
    - "results/crossover_table.md is the canonical crossover surface: a markdown table with columns (n, d, dataset, impl, algorithm, median_warm_s, speedup_vs_best_cpu, accuracy, oom)"
    - "The kill-criterion evaluation has been adjudicated at a blocking human-verify checkpoint after reading the crossover table; the outcome (PROCEED or PIVOT) is recorded in STATE.md and ROADMAP.md"
    - "If any per-phase (1–6) speed data point conflicts with the Phase-7 surface for the same (n, d), a reconciliation note is written in results/crossover_table.md explaining the discrepancy"
    - "No cell in crossover_table.md shows a speedup computed by comparing ET to RF or GPU ET to cuML RF — only ET-vs-ET and RF-vs-RF speedup columns appear"
  artifacts:
    - path: "python/benchmarks/visualize_surface.py"
      provides: "heatmap generator (PNG) + markdown table builder from crossover_results.json"
      contains: "build_surface_table"
    - path: "results/crossover_results.json"
      provides: "canonical per-cell measurement results (all cells, all impls)"
      contains: "manifest"
    - path: "results/crossover_table.md"
      provides: "human-readable crossover surface — the primary published artifact (BENCH-03)"
      contains: "speedup_vs_best_cpu"
    - path: "results/crossover_surface.png"
      provides: "heatmap visualization of the ET crossover surface (GPU win/lose/OOM per cell)"
      min_lines: 0
  key_links:
    - from: "python/benchmarks/visualize_surface.py"
      to: "results/crossover_results.json"
      via: "reads the JSON; builds the markdown table and the heatmap PNG"
      pattern: "crossover_results.json"
    - from: "results/crossover_table.md"
      to: ".planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md"
      via: "kill-criterion win condition evaluated against the pre-registered threshold (5% speedup + 1% accuracy delta)"
      pattern: "KILL CRITERION"
---

<objective>
Run the full measurement, publish the crossover surface, and adjudicate the kill criterion.

This plan completes the Gate 3 study: (1) executes run_crossover_study() on the dev host to
produce the full results JSON, (2) runs visualize_surface.py to produce the canonical markdown
table and the heatmap PNG, (3) presents the surface at a blocking human-verify checkpoint that
evaluates the pre-registered kill criterion and records the outcome (PROCEED or PIVOT) in
STATE.md and ROADMAP.md.

Purpose: After this plan the project knows whether the GPU ET training advantage exists on the
RTX 4060 Ti (end-to-end from numpy, on the pre-registered grid). If PROCEED: Phase 8 is
unblocked. If PIVOT: the pivot path (SHAP/determinism layer on cuML, Phases 8–9 still executable
on CPU backend) is documented and executed.

Output:
  - python/benchmarks/visualize_surface.py
  - results/crossover_results.json, crossover_results.csv, crossover_table.md, crossover_surface.png
  - Updated STATE.md and ROADMAP.md with kill-criterion outcome

Decisions implemented: D-4 (kill criterion adjudicated at blocking human-verify checkpoint after
measurement — per LOCKED decision this plan delivers that checkpoint), D-3 (deterministic=False
fast path is the primary speed claim in the surface; deterministic=True reported as a separate
column), D-1 (pre-registration committed before measurement — enforced by wave dependency on
07-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md
@.planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md
@.planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md
@python/benchmarks/crossover_study.py
@python/benchmarks/crossover_manifest.py
@python/benchmarks/grid_spec.py
</context>

## Assumptions

- D-4 LOCKED: the kill criterion checkpoint is a blocking human gate — it cannot be automated. The harness produces the surface; the human evaluates whether the kill criterion fires. Alternative (automated pivot): rejected — this is a strategic project decision.
- Phase 5 (05-06) study results exist from when Phase 5 executes. If Phase 5 has not yet executed at Phase 7 execution time (phases run in order but this plan is written ahead of time), the reconciliation note reads "Phase-5 study not yet executed; no prior data point to reconcile."
- The full grid measurement is long-running (hours). This plan's Task 1 runs the harness; it is expected to be a long-running dev-host operation. The verify step checks that the JSON exists and is complete (all cells present), not that it runs instantly.
- Alternative for Higgs dataset: if OpenML download fails, make_classification(n_samples=1_000_000, n_features=28, random_state=DATASET_SEED) is the pre-registered fallback. Document the substitution in results/crossover_table.md.

<tasks>

<task type="auto">
  <name>Task 1: visualize_surface.py — markdown table builder + heatmap generator from results JSON</name>
  <files>python/benchmarks/visualize_surface.py</files>
  <read_first>
    - .planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md — visualize_surface.py pattern (build_surface_table function signature, build_heatmap function, RESULTS_PATH constant)
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Architecture Patterns §Architecture Diagram (output file names and formats: crossover_results.json → crossover_table.md primary canonical, crossover_surface.png supplemental), Anti-Patterns (no ET-vs-RF speedup in the table), Kill Criterion specification (WIN_THRESHOLD=0.05, ACCURACY_THRESHOLD=0.01, best_cpu_et_warm_median definition)
    - .planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md — Section 7 pass bar (win condition for the heatmap green cells), Section 8 kill criterion wording (quoted in the markdown table header)
  </read_first>
  <action>
    Create python/benchmarks/visualize_surface.py:

    Module docstring: "Generate crossover surface visualization from results/crossover_results.json.
    The markdown table (crossover_table.md) is the CANONICAL artifact; the heatmap (crossover_surface.png)
    is supplemental. Fairness: speedup columns are computed ONLY within the same algorithm group
    (ET-vs-ET, RF-vs-RF). cuML RF speedup appears ONLY in the RF-vs-RF sub-table."

    Constants: RESULTS_PATH = Path("results/crossover_results.json"),
    TABLE_PATH = Path("results/crossover_table.md"),
    PNG_PATH = Path("results/crossover_surface.png"),
    CSV_OUT_PATH = Path("results/crossover_results.csv"),
    WIN_THRESHOLD = 0.05 (from 07-PRE-REGISTRATION.md Section 7),
    ACCURACY_THRESHOLD = 0.01.

    build_surface_table(results: list[dict]) -> str: constructs the markdown table. The table has
    two sections: "ET Group (ExtraTrees — primary crossover)" and "RF Group (RandomForest)".
    Columns for each section: n, d, dataset, impl, median_warm_s, speedup_vs_best_cpu_warm (for ET
    group: best CPU = min of sklearn_et_warm and sklearnex_et_warm; for RF group: best CPU = min of
    sklearn_rf_warm and sklearnex_rf_warm), accuracy, oom. Win cells: speedup_vs_best_cpu_warm >
    1 + WIN_THRESHOLD and not oom and |accuracy - sklearn_reference_accuracy| < ACCURACY_THRESHOLD,
    marked with "WIN" in the speedup column. OOM cells marked "OOM". The table includes a header
    block quoting the kill-criterion wording verbatim from 07-PRE-REGISTRATION.md Section 8, so
    the table is self-contained for the human-verify checkpoint. Speedup_vs_best_cpu_warm is
    NEVER computed across algorithm groups — an ET row's speedup is always ET-baseline/ET-impl,
    never RF.

    Reconciliation section: the table ends with a "## Reconciliation with Per-Phase Studies"
    section. For each per-phase study data point (Phase-5 comparative_study.py result on
    Covertype, if available), the section compares it to the Phase-7 surface value for the same
    (n, d) and explains any discrepancy.

    build_heatmap(et_results: list[dict], out_path: Path) -> None: builds a matplotlib heatmap
    of the ET group only (the primary crossover surface). Rows = n values, columns = d values.
    Cell color: green if speedup > WIN_THRESHOLD and accuracy parity, red if GPU loses, grey if
    OOM, white if no Sylva GPU ET data. Uses matplotlib.pyplot.imshow or seaborn.heatmap.
    Attempts import of matplotlib; if unavailable, logs "matplotlib not installed; skipping
    heatmap (crossover_table.md is the canonical artifact)" and returns.

    main(results_path=RESULTS_PATH, output_dir=Path("results")): reads JSON; separates ET rows
    from RF rows by impl.algorithm field; calls build_surface_table; writes crossover_table.md;
    calls build_heatmap for ET rows; saves PNG. Exits with code 0. Prints "Surface table written
    to results/crossover_table.md" on success.

    if __name__ == "__main__": main().
  </action>
  <verify>
    <automated>cd python && python -c "import ast; ast.parse(open('benchmarks/visualize_surface.py').read()); print('parse OK')" && echo "PASS: visualize_surface.py parseable"</automated>
  </verify>
  <done>visualize_surface.py parses and imports cleanly. build_surface_table produces a markdown string with an ET Group section, a RF Group section, speedup_vs_best_cpu_warm column, WIN/OOM markers, and a verbatim kill-criterion quote. The heatmap gracefully skips if matplotlib is unavailable. No ET-vs-RF speedup computation exists in the code.</done>
</task>

<task type="auto">
  <name>Task 2: Full measurement run — execute crossover_study.py on the dev host and produce all surface artifacts</name>
  <files>results/crossover_results.json, results/crossover_results.csv, results/crossover_table.md, results/crossover_surface.png</files>
  <read_first>
    - python/benchmarks/crossover_study.py — run_crossover_study function signature and --smoke flag
    - .planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md — Section 1 (full grid: 24 synthetic cells), Section 2 (real anchors), Section 6 (OOM policy: every cell in JSON), Section 5 (timing protocol reminder)
    - results/crossover_results.json (if the file already exists from a smoke run — check before overwriting)
  </read_first>
  <action>
    On the dev host (Windows, RTX 4060 Ti, CUDA 12.8), execute the full crossover study:

    Step 1: Verify the pre-registration commit exists:
    git log --oneline -- .planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md
    The commit must predate the current time. If it does not exist, STOP — the pre-registration
    gate (Plan 01 Task 1) was not completed; do not run measurements.

    Step 2: Run the full measurement (LONG-RUNNING — expected hours):
    cd python && python benchmarks/crossover_study.py
    (no --smoke flag; full grid)

    Step 3: After the harness completes, verify completeness of results/crossover_results.json:
    The JSON must contain at least 24 rows for the synthetic grid (one per cell) times the number
    of available impls. OOM rows count — their absence would mean cells were silently dropped.
    Verify: python -c "import json; r=json.load(open('results/crossover_results.json')); cells=set((x['n'],x['d']) for x in r['results'] if x.get('dataset','').startswith('synthetic')); expected=set((n,d) for n in [10000,50000,100000,250000,500000,1000000] for d in [20,50,100,200]); missing=expected-cells; print('missing cells:', missing or 'none')"

    Step 4: Run the visualization to produce the canonical table and heatmap:
    cd python && python benchmarks/visualize_surface.py

    Step 5: Verify the canonical table exists:
    test -f results/crossover_table.md and results/crossover_table.md contains the kill-criterion
    wording and the speedup_vs_best_cpu_warm column.

    Step 6: Write the Covertype real anchor and (if available) Higgs/make_classification 1M×28
    anchor rows to the results JSON. These are separate from the synthetic grid; run_crossover_study
    handles them if make_covertype_dataset is called.
  </action>
  <verify>
    <automated>test -f results/crossover_results.json && python -c "import json; data=json.load(open('results/crossover_results.json')); cells=set((x['n'],x['d']) for x in data.get('results',[]) if str(x.get('dataset','')).startswith('synthetic')); expected=set((n,d) for n in [10000,50000,100000,250000,500000,1000000] for d in [20,50,100,200]); missing=expected-cells; assert not missing, f'Missing cells: {missing}'; print(f'All {len(expected)} synthetic cells present')" && test -f results/crossover_table.md && echo "crossover_table.md exists"</automated>
    <human-check>Visually confirm that results/crossover_table.md: (1) has an "ET Group" section and an "RF Group" section; (2) no ET-vs-RF speedup row exists; (3) OOM cells are present (not absent) for large n/d cells where GPU memory was exceeded; (4) every speed row has an accuracy value beside it.</human-check>
  </verify>
  <done>results/crossover_results.json contains all 24 pre-registered synthetic cells for every available implementation (OOM rows included). results/crossover_table.md exists with the ET/RF sub-tables, speedup column, accuracy column, and the verbatim kill-criterion quote. results/crossover_surface.png exists (or is absent with a logged reason if matplotlib was unavailable).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Kill-criterion adjudication — PROCEED to Phase 8 or PIVOT to SHAP/determinism layer</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>After the human evaluates the kill criterion and provides the resume signal, update .planning/STATE.md under "## Accumulated Context > Decisions" with the binary outcome: either "Phase 7 crossover: CONFIRMED. Win region found at cells (n=X, d=Y). Phase 8 UNBLOCKED." or "Phase 7 kill criterion fired: no cell showed GPU ET >= 5% faster than strongest CPU baseline with accuracy parity and IQR significance. Pivoting to SHAP/determinism layer." Update .planning/ROADMAP.md Phase 7 status to Complete with the outcome noted. Do not update these files until the human resume signal is received.</action>
  <verify><automated>grep -q "Phase 7" .planning/STATE.md && echo "STATE.md updated" || echo "WARN: STATE.md not yet updated — checkpoint not completed"</automated></verify>
  <done>STATE.md contains the binary kill-criterion outcome (CONFIRMED or fired). ROADMAP.md Phase 7 is marked Complete. The outcome is durable and readable without ambiguity.</done>
  <what-built>The complete crossover surface in results/crossover_table.md: a per-cell breakdown of Sylva GPU ET warm fit time vs the strongest CPU ET baseline (sklearn n_jobs=-1 and/or sklearnex ET), with speedup_vs_best_cpu_warm, accuracy delta, and OOM flags. The pre-registered kill criterion is reproduced verbatim in the table header.</what-built>
  <how-to-verify>
    1. Open results/crossover_table.md. Read the ET Group section.

    2. Evaluate the pre-registered kill criterion (from 07-PRE-REGISTRATION.md Section 8):
       "Does any cell in the pre-registered (n×d) grid show Sylva GPU ExtraTreesClassifier warm
       fit(X, y) time — measured end-to-end from a host float64 numpy array, including dtype
       coercion + H2D + quantization — beating the BEST of {sklearn ET n_jobs=-1, sklearnex ET}
       warm times by at least 5% AND exceeding the observed run-to-run IQR (statistically
       distinguishable), while maintaining test-set accuracy within 1% of sklearn ET?"

    3. WIN condition (for each cell): speedup_vs_best_cpu_warm > 1.05 AND speedup exceeds the
       iqr_warm_s of Sylva GPU ET for that cell AND accuracy_delta < 0.01 AND oom=False for both
       Sylva GPU ET and the best CPU baseline.

    4. If YES (at least one WIN cell exists):
       Record the crossover coordinates (n, d values of WIN cells).
       Update .planning/STATE.md under "## Accumulated Context > Decisions": add
       "Phase 7 crossover: CONFIRMED. Win region found at cells (n=X, d=Y, ...). Phase 8 UNBLOCKED."
       Update .planning/ROADMAP.md Phase 7 status to Complete with a one-line summary of the
       crossover coordinates. Publish the surface (crossover_table.md is the published artifact).
       Reply "PROCEED" to resume Phase 8 planning.

    5. If NO (no cell satisfies all win conditions):
       The KILL CRITERION fires. Do NOT continue broad GPU training build-out.
       Update .planning/STATE.md: "Phase 7 kill criterion fired: no cell in the pre-registered
       (n×d) grid showed GPU ET warm fit time >= 5% faster than the strongest CPU baseline with
       accuracy parity and statistical significance. Core premise (GPU ET beats optimized CPU on
       large dense workloads, end-to-end from numpy, on RTX 4060 Ti) is NOT confirmed. Pivoting
       to the SHAP/determinism layer."
       Update .planning/ROADMAP.md: Mark Phase 7 as Complete (gate evaluated, outcome: pivot).
       Note that Phase 8 (exact tree SHAP) and Phase 9 (packaging) remain executable on the CPU
       backend and on cuML if available. The ForestIR is stable; SHAP consuming the IR is
       independent of the GPU training result.
       Reply "PIVOT" with a brief note on which Phase-8/9 items remain valuable and which
       GPU-training build-out items are descoped.
  </how-to-verify>
  <resume-signal>Reply with "PROCEED" (crossover confirmed — record coordinates) or "PIVOT" (kill criterion fired — record pivot decision). Include the n,d coordinates of the WIN cells (if PROCEED) or a brief rationale (if PIVOT). Update STATE.md and ROADMAP.md accordingly. Do NOT auto-approve — this is a strategic project decision that requires human judgment on the published crossover surface.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crossover_results.json → crossover_table.md | Visualization must not introduce ET-vs-RF speedup comparisons or omit OOM cells from the table |
| crossover_table.md → kill-criterion checkpoint | The surface presented to the human must be the complete, unmodified measurement result — no post-hoc selection of cells |
| kill-criterion outcome → STATE.md / ROADMAP.md | The recorded outcome must match the binary decision (PROCEED or PIVOT) without hedging |
| cuML / GPL source observation | Benchmarking cuML means running it and observing output; never copying its source — Apache-2.0 compliance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Repudiation | visualize_surface.py produces an ET-vs-RF speedup column | mitigate | Algorithm tag separation in build_surface_table: ET speedup is computed only against ET baselines; code review during executor pass must verify no RF reference in ET speedup formula |
| T-07-11 | Tampering | OOM cells omitted from crossover_table.md (makes the surface look complete) | mitigate | build_surface_table iterates ALL rows from the JSON including oom=True rows; OOM marker in the speedup column is explicit; the verify step asserts OOM rows are present |
| T-07-12 | Tampering | Measurement run before pre-registration commit | mitigate | Task 2 Step 1 explicitly checks git log for the pre-registration commit; if the commit is absent the measurement is stopped |
| T-07-13 | Repudiation | Kill-criterion outcome is vague (no binary decision recorded) | mitigate | Checkpoint task requires "PROCEED" or "PIVOT" as the resume signal; STATE.md and ROADMAP.md must be updated with the binary outcome before resuming |
| T-07-14 | Information Disclosure | GPL/Snap ML source copied while studying cuML behavior | mitigate | Only cuML behavior (output timings, accuracy) is observed; cuML source is never read, copied, or adapted; Sylva algorithms are derived from papers, not cuML source (Apache-2.0 compliance, PROJECT.md constraint) |
| T-07-SC | Tampering | No new pip installs in this plan | accept | All packages (matplotlib, seaborn for heatmap) were gated at Plan 02 checkpoint; no new installs in Plan 03 |
</threat_model>

<verification>
- results/crossover_results.json exists and contains all 24 synthetic cells for every available implementation including OOM rows.
- results/crossover_table.md exists, has an "ET Group" section, a "RF Group" section, the verbatim kill-criterion wording, a speedup_vs_best_cpu_warm column (ET group only), an accuracy column beside every speed cell, and OOM markers for all OOM cells.
- No row in crossover_table.md shows a speedup computed by comparing Sylva ET to cuML RF or to any RF baseline.
- The kill-criterion checkpoint is recorded as blocking-human (not auto-approved).
- STATE.md contains either "Phase 7 crossover: CONFIRMED" or "Phase 7 kill criterion fired" after the checkpoint completes.
- ROADMAP.md Phase 7 status is updated to Complete with the kill-criterion outcome noted.
</verification>

<success_criteria>
- The complete (n×d) crossover surface is published in results/crossover_table.md — an honest map of where GPU ET wins and where it does not, on the pre-registered grid, with accuracy beside every speed cell and OOM cells honestly present.
- The kill criterion has been adjudicated by a human and the outcome (PROCEED or PIVOT) is durably recorded in STATE.md and ROADMAP.md.
- If PROCEED: Phase 8 is unblocked; the crossover coordinates are known.
- If PIVOT: the pivot path (SHAP/determinism layer, Phases 8–9 on CPU backend) is recorded and Phase 8 is still planned (exact SHAP is valuable independent of GPU training).
- Every fairness rule is provably satisfied by the published surface: like-for-like algorithms, end-to-end from numpy, cold/warm separated, accuracy beside speed, strongest CPU baseline, OOM honest, versions pinned.
</success_criteria>

<output>
Create `.planning/phases/07-crossover-benchmark-gate-3/07-03-SUMMARY.md` when done.
</output>
