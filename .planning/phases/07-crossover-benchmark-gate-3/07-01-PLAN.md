---
phase: 07-crossover-benchmark-gate-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md
  - python/benchmarks/__init__.py
  - python/benchmarks/crossover_manifest.py
  - python/benchmarks/grid_spec.py
  - python/tests/test_crossover_manifest.py
  - python/tests/test_crossover_fairness_rules.py
autonomous: true
requirements: [BENCH-01, BENCH-02, BENCH-03]
must_haves:
  truths:
    - "07-PRE-REGISTRATION.md is committed to git BEFORE any measurement cell runs — the git timestamp is the tamper evidence that the protocol was not chosen retroactively"
    - "The pre-registration document contains verbatim the exact kill-criterion wording, the full (n×d) grid (n ∈ {10k, 50k, 100k, 250k, 500k, 1M} × d ∈ {20, 50, 100, 200}), real anchors (Covertype full, Higgs 1M×28 with make_classification fallback), hyperparameters, win threshold (5%), accuracy threshold (1%), and pivot path"
    - "crossover_manifest.py MANIFEST dict records grid_n_values, grid_d_values, n_repeats_warm, win_threshold, accuracy_threshold, hardware/CUDA/driver pins, and baseline version placeholders that are filled at measurement time"
    - "grid_spec.py defines SYNTHETIC_GRID as the full Cartesian product of the pre-registered n and d values; dataset loaders keep X as float64 at load time (coercion happens inside the timed region, never here)"
    - "test_crossover_fairness_rules.py asserts structurally that ET and RF impl sets are disjoint, all sklearn factories use n_jobs=-1, and cuML does not appear in the ET comparison group"
    - "test_crossover_manifest.py asserts that MANIFEST has all required keys and that grid_n_values and grid_d_values match the pre-registered values exactly"
  artifacts:
    - path: ".planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md"
      provides: "frozen benchmark protocol committed before measurement — the anti-p-hacking contract"
      contains: "KILL CRITERION"
    - path: "python/benchmarks/crossover_manifest.py"
      provides: "pinned version manifest for the crossover study"
      contains: "MANIFEST"
    - path: "python/benchmarks/grid_spec.py"
      provides: "pre-registered (n×d) grid + dataset loaders"
      contains: "SYNTHETIC_GRID"
    - path: "python/tests/test_crossover_fairness_rules.py"
      provides: "structural fairness assertions (ET-vs-ET, n_jobs=-1, no cuML in ET group)"
      contains: "isdisjoint"
    - path: "python/tests/test_crossover_manifest.py"
      provides: "manifest completeness + grid-matches-preregistration assertion"
      contains: "grid_n_values"
  key_links:
    - from: "python/benchmarks/grid_spec.py"
      to: ".planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md"
      via: "SYNTHETIC_GRID values must match the pre-registered grid exactly — divergence is p-hacking"
      pattern: "SYNTHETIC_GRID"
    - from: "python/tests/test_crossover_manifest.py"
      to: "python/benchmarks/crossover_manifest.py"
      via: "imports MANIFEST; asserts grid_n_values and grid_d_values match pre-registered list"
      pattern: "MANIFEST"
    - from: "python/tests/test_crossover_fairness_rules.py"
      to: "python/benchmarks/grid_spec.py"
      via: "imports ET_IMPLS, RF_IMPLS from grid_spec; asserts disjointness and n_jobs=-1"
      pattern: "ET_IMPLS|RF_IMPLS"
---

<objective>
Lock the benchmark protocol before any measurement can occur.

This plan produces and COMMITS the pre-registration document (07-PRE-REGISTRATION.md), the manifest
(crossover_manifest.py), the grid spec (grid_spec.py), and the fairness-rule unit tests — in that
order. The git timestamp on the pre-registration commit is the only tamper evidence that the protocol
was not chosen retroactively after seeing results. Nothing in Plans 02 or 03 may run until this
commit exists.

Purpose: Encode the anti-p-hacking contract (Pitfalls 13, 3) and the structural fairness rules
(ET-vs-ET, n_jobs=-1, cuML labeled differently) as code and a frozen document — so the kill
criterion cannot be moved after the fact and the fairness rules cannot be accidentally violated in
the harness.

Output:
  - 07-PRE-REGISTRATION.md (frozen protocol, kill-criterion wording, pivot path) — COMMITTED first
  - crossover_manifest.py (MANIFEST dict with all required fields + hardware/baseline placeholders)
  - grid_spec.py (SYNTHETIC_GRID Cartesian product + real anchors + dataset loaders, float64 output)
  - test_crossover_manifest.py (manifest completeness + grid match assertion)
  - test_crossover_fairness_rules.py (structural ET/RF disjointness, n_jobs=-1, no cuML in ET group)

Decisions implemented: D-1 (pre-registration committed before measurement), D-6 (fairness rules as
structural gates), D-5 (harness generalizes Phase-5 — leave Phase-5 files untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md
@.planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md
@.planning/phases/07-crossover-benchmark-gate-3/07-VALIDATION.md
@python/tests/parity/conftest.py
@python/tests/parity/datasets.py
</context>

## Assumptions

- D-1 LOCKED: pre-registration committed before measurement. The commit containing 07-PRE-REGISTRATION.md is the blocking gate for Plans 02 and 03.
- D-6 LOCKED: fairness rules encoded as structural unit tests (not documentation). test_crossover_fairness_rules.py fails fast if any factory violates n_jobs=-1 or ET/RF disjointness.
- D-5 LOCKED: Phase-5 files (comparative_study.py, study_manifest.py) are not touched. This plan introduces new files only.
- grid_spec.py exports ET_IMPLS and RF_IMPLS with placeholder factories that instantiate correctly so fairness tests pass before the full harness (Plan 02) is written. The factories at this stage use sklearn as the reference; Sylva factories are filled in Plan 02.
- Alternative (if ET_IMPLS with Sylva factories cannot be defined without Plan 02's crossover_study.py): move Sylva-specific factory definitions to crossover_study.py and keep grid_spec.py as pure grid/loader definitions; adjust fairness test imports accordingly.

<tasks>

<task type="auto">
  <name>Task 1: Pre-registration document (07-PRE-REGISTRATION.md) — committed before any measurement</name>
  <files>.planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md</files>
  <read_first>
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Section "Pre-Registration Document Structure" (10 required sections), Section "Kill Criterion — Full Specification" (exact trigger condition, WIN_THRESHOLD=0.05, ACCURACY_THRESHOLD=0.01, pivot path text), Section "Pre-Registration Grid Recommendation"
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Locked Decisions (from USER CONSTRAINTS section): timing protocol, hyperparameter set, baseline labels
    - .planning/ROADMAP.md — Phase 7 Success Criterion 4 (KILL CRITERION exact wording from ROADMAP)
  </read_first>
  <action>
    Create .planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md as a Markdown
    document containing ALL ten sections specified in the RESEARCH.md §Pre-Registration Document
    Structure. The document is the anti-p-hacking contract and must be committed to git before any
    measurement cell is run. Content requirements by section:

    Section 1 — Grid specification: n ∈ {10_000, 50_000, 100_000, 250_000, 500_000, 1_000_000},
    d ∈ {20, 50, 100, 200}. Full Cartesian product = 24 synthetic cells. State explicitly that the
    grid is run in full; cells are NOT dropped retroactively. Include a markdown table listing all
    24 cells.

    Section 2 — Real anchors: Covertype full (sklearn.datasets.fetch_covtype, ~581_012 rows × 54
    features). Higgs subset: rows 0..1_000_000 of the full Higgs dataset from OpenML ID 23512,
    seed-shuffled with DATASET_SEED=2025_06_20 before splitting. Fallback: if OpenML is unavailable,
    use make_classification(n_samples=1_000_000, n_features=28, random_state=DATASET_SEED); document
    the substitution.

    Section 3 — Fixed hyperparameters (identical across all implementations):
    n_estimators=200, max_depth=12, max_features="sqrt", min_samples_split=2, min_samples_leaf=1,
    bootstrap=False for ET (ExtraTrees), bootstrap=True for RF (RandomForest), criterion="gini"
    for classifiers. sklearn baselines: n_jobs=-1 (REQUIRED — using any other value is a protocol
    violation). State this as a prohibition.

    Section 4 — Baseline set and labels:
    ET group: sklearn ExtraTreesClassifier (n_jobs=-1, is_reference=True), sklearnex
    ExtraTreesClassifier if available (labeled "oneDAL-accelerated sklearn ET"), Sylva GPU ET
    (device='cuda', deterministic=False). PROHIBITION: cuML must NOT appear in the ET group — cuML
    has no GPU ExtraTrees; its absence is documented, not hidden.
    RF group: sklearn RandomForestClassifier (n_jobs=-1), sklearnex RF if available, cuML RF
    (labeled "cuML RF — a different algorithm from ET, reported in RF-vs-RF group only"), Sylva
    GPU RF (device='cuda', deterministic=False).
    Sylva CPU ET/RF (device='cpu') included for transfer-cost analysis (not part of the crossover
    surface comparison).
    Sylva deterministic=True results reported as a separate column with Phase-6 overhead fraction.

    Section 5 — Timing protocol: time.perf_counter() wall-clock. Timed region definition (per D-2
    LOCKED): the clock START is placed BEFORE X_f64.astype(np.float32); the clock END is placed
    AFTER model.fit(X32, y) returns. This means dtype coercion + H2D + quantization + training are
    ALL inside the timed region. State this as an absolute prohibition: starting the timer after
    dtype coercion is a protocol violation. cold = 1 first call per (n, d) cell. warm = 5
    subsequent calls on the SAME (n, d) data. Report median_cold_s, median_warm_s, iqr_warm_s.

    Section 6 — OOM policy: catch MemoryError and RuntimeError containing "out of memory"; record
    oom=True and error message; continue the grid loop. Do NOT remove OOM cells from the published
    surface — their presence is part of the honest result.

    Section 7 — Pass bar: a win region exists if for at least one (n, d) cell in the pre-registered
    grid, ALL of: (a) Sylva GPU ET warm median time < min(sklearn_ET_warm_median, sklearnex_ET_warm_median)
    × (1 - 0.05), (b) the speedup exceeds the observed IQR of Sylva GPU ET warm times (statistically
    distinguishable), (c) test-set accuracy delta |sylva_acc - sklearn_acc| < 0.01, (d) neither
    Sylva GPU ET nor the best CPU baseline is OOM for this cell.

    Section 8 — KILL CRITERION (exact wording, to be reproduced verbatim in the checkpoint task):
    "If no cell in the pre-registered (n×d) grid shows Sylva GPU ExtraTreesClassifier warm fit(X, y)
    time — measured end-to-end from a host float64 numpy array, including dtype coercion + H2D +
    quantization — beating the BEST of {sklearn ET n_jobs=-1, sklearnex ET} warm times by at least
    5% AND exceeding the observed run-to-run IQR (statistically distinguishable), while maintaining
    test-set accuracy within 1% of sklearn ET, then the core premise of the GPU advantage is not
    confirmed on this hardware. The project shall pivot to the SHAP/determinism layer (which can ride
    on cuML) or to upstreaming rather than continuing broad GPU training build-out."

    Section 9 — Hardware/version snapshot: GPU model RTX 4060 Ti, CUDA 12.8, driver 595.79,
    compute cap sm_89. Sylva git SHA recorded at measurement time. sklearn 1.9.0. Other baseline
    versions recorded at the baseline-availability checkpoint (Plan 02). All pins are extended into
    crossover_manifest.py MANIFEST.

    Section 10 — Reconciliation note: "This study is the authoritative Phase-7 crossover. Any
    conflict between a per-phase study (Phases 1–6) speed number and the Phase-7 surface number for
    the same (n, d) point will be explained in the crossover report. The Phase-5 study (comparative_study.py,
    plan 05-06) reported a preliminary end-to-end result on Covertype + large synthetic; if the
    Phase-7 surface contradicts that data point, the discrepancy will be explained."

    After writing the file, COMMIT it immediately using git with message:
    "bench(07): pre-register crossover benchmark protocol — frozen before measurement"
    The commit timestamp is the tamper evidence. This commit must exist before any crossover
    measurement cell is run.
  </action>
  <verify>
    <automated>git log --oneline -1 -- .planning/phases/07-crossover-benchmark-gate-3/07-PRE-REGISTRATION.md | grep -q "pre-register" && echo "pre-registration committed" || echo "FAIL: pre-registration not committed"</automated>
  </verify>
  <done>07-PRE-REGISTRATION.md exists on disk AND has a git commit timestamp before any measurement cell runs. The document contains all 10 required sections including the verbatim kill-criterion wording and the explicit prohibition on starting the timer after dtype coercion. The commit message includes "pre-register".</done>
</task>

<task type="auto">
  <name>Task 2: crossover_manifest.py + grid_spec.py + fairness unit tests</name>
  <files>python/benchmarks/__init__.py, python/benchmarks/crossover_manifest.py, python/benchmarks/grid_spec.py, python/tests/test_crossover_manifest.py, python/tests/test_crossover_fairness_rules.py</files>
  <read_first>
    - .planning/phases/07-crossover-benchmark-gate-3/07-PATTERNS.md — crossover_manifest.py pattern (MANIFEST dict shape extending conftest VERSION_MANIFEST), grid_spec.py pattern (Dataset NamedTuple, make_grid_dataset, SYNTHETIC_GRID, N_VALUES, D_VALUES, CLF_HYPERPARAMS import note), test_crossover_manifest.py pattern (required keys list, grid-match assertion), test_crossover_fairness_rules.py pattern (test_no_et_vs_rf_cross, test_sklearn_baselines_have_n_jobs_minus_1, test_cuml_labeled_different_algorithm)
    - python/tests/parity/conftest.py — VERSION_MANIFEST shape and _sylva_commit() helper to reuse
    - python/tests/parity/datasets.py — DATASET_SEED, CLF_HYPERPARAMS, RF_CLF_HYPERPARAMS, Dataset NamedTuple to import or mirror
    - .planning/phases/07-crossover-benchmark-gate-3/07-RESEARCH.md — Locked Decision D-2 (timing contract: X stays float64 in grid_spec, coercion inside timed_fit), Standard Stack table (MANIFEST field names), Package Legitimacy Audit (sklearnex and cuml are ASSUMED — gate behind checkpoint)
  </read_first>
  <action>
    Create python/benchmarks/__init__.py as an empty package marker (no content except a module
    docstring stating "Phase-7 crossover benchmark harness — do not confuse with Phase-5 benchmarks").

    Create python/benchmarks/crossover_manifest.py with a MANIFEST dict that EXTENDS the
    VERSION_MANIFEST shape from python/tests/parity/conftest.py. Import _sylva_commit from conftest
    or duplicate the subprocess git-rev-parse pattern. Add these fields beyond the base shape:
    sklearnex_version (string, default "not yet resolved — see checkpoint:human-verify in plan 07-02"),
    cuml_version (string, default "not yet resolved — see checkpoint:human-verify in plan 07-02"),
    gpu_model ("RTX 4060 Ti"), cuda_version ("12.8"), driver_version ("595.79"), compute_cap ("sm_89"),
    grid_n_values ([10_000, 50_000, 100_000, 250_000, 500_000, 1_000_000]),
    grid_d_values ([20, 50, 100, 200]),
    n_repeats_cold (1), n_repeats_warm (5),
    win_threshold (0.05), accuracy_threshold (0.01),
    dataset_seed (imported from python.tests.parity.datasets or hardcoded 2025_06_20),
    pre_registration_doc ("07-PRE-REGISTRATION.md").

    Create python/benchmarks/grid_spec.py with:
    - Imports: NamedTuple from typing, numpy as np, sklearn.datasets.make_classification +
      fetch_covtype + make_regression, sklearn.model_selection.train_test_split. Import DATASET_SEED,
      CLF_HYPERPARAMS, RF_CLF_HYPERPARAMS from python.tests.parity.datasets (or from relative path).
    - Dataset NamedTuple with fields: name (str), X_train (np.ndarray), X_test (np.ndarray),
      y_train (np.ndarray), y_test (np.ndarray), task (str), n_classes (int). CRITICAL: X_train and
      X_test are float64 — dtype coercion to float32 happens INSIDE timed_fit() per D-2, NEVER here.
      Add a comment on the float64 fields documenting this constraint.
    - Constants: N_VALUES = [10_000, 50_000, 100_000, 250_000, 500_000, 1_000_000],
      D_VALUES = [20, 50, 100, 200], SYNTHETIC_GRID = [(n, d) for n in N_VALUES for d in D_VALUES].
      These must be identical to the lists in crossover_manifest.py MANIFEST and the pre-registration
      document. A comment must state: "These values are PRE-REGISTERED in 07-PRE-REGISTRATION.md.
      Do NOT modify after the pre-registration commit without re-registering."
    - make_grid_dataset(n, d, seed=DATASET_SEED) function: calls make_classification with
      n_samples=n, n_features=d, n_informative=max(2, d // 3), n_redundant=max(1, d // 10),
      random_state=seed; train_test_split with test_size=0.2, random_state=seed; returns Dataset.
      X stays float64.
    - make_covertype_dataset(seed=DATASET_SEED) function: calls fetch_covtype(as_frame=False);
      formats as Dataset (n_classes = number of unique target values); X stays float64.
    - BenchmarkImpl dataclass with fields: name (str), algorithm (str — "et" or "rf"), backend (str
      — "cpu" or "gpu"), is_reference (bool), factory (callable returning an unfitted estimator).
      __post_init__ asserts algorithm in ("et", "rf").
    - ET_IMPLS and RF_IMPLS lists: populated with sklearn-only factories at this stage (Sylva GPU
      factories are added in crossover_study.py in Plan 02). ET_IMPLS contains at minimum:
      BenchmarkImpl("sklearn_et", "et", "cpu", True, lambda: ExtraTreesClassifier(**{**CLF_HYPERPARAMS, "n_jobs": -1})).
      RF_IMPLS contains at minimum:
      BenchmarkImpl("sklearn_rf", "rf", "cpu", True, lambda: RandomForestClassifier(**{**RF_CLF_HYPERPARAMS, "n_jobs": -1})).
      Both lists export from grid_spec so that test_crossover_fairness_rules.py can import them.
      (The full impl list with Sylva GPU entries is assembled in crossover_study.py in Plan 02 by
      importing from grid_spec and appending.)

    Create python/tests/test_crossover_manifest.py (follows PATTERNS.md):
    - Import MANIFEST from benchmarks.crossover_manifest.
    - test_manifest_has_required_keys: asserts the set {"python", "sklearn", "numpy", "sylva_commit",
      "dataset_seed", "sklearnex_version", "cuml_version", "gpu_model", "cuda_version",
      "driver_version", "grid_n_values", "grid_d_values", "n_repeats_warm", "win_threshold",
      "accuracy_threshold"} is a subset of MANIFEST.keys().
    - test_manifest_grid_matches_preregistration: asserts MANIFEST["grid_n_values"] ==
      [10_000, 50_000, 100_000, 250_000, 500_000, 1_000_000] and MANIFEST["grid_d_values"] ==
      [20, 50, 100, 200].

    Create python/tests/test_crossover_fairness_rules.py (follows PATTERNS.md):
    - Import ET_IMPLS, RF_IMPLS from benchmarks.grid_spec.
    - Add FP-1..FP-6 fairness protocol tag block at the top of the file as a module docstring
      (mirror the tag pattern from test_distributional_parity.py).
    - test_no_et_vs_rf_cross: et_names = {impl.name for impl in ET_IMPLS};
      rf_names = {impl.name for impl in RF_IMPLS};
      assert et_names.isdisjoint(rf_names), message referencing PITFALL 13 and D-6.
    - test_sklearn_baselines_have_n_jobs_minus_1: for each impl in ET_IMPLS + RF_IMPLS where
      "sklearn" in impl.name: assert impl.factory().n_jobs == -1.
    - test_cuml_labeled_different_algorithm: et_names = {impl.name for impl in ET_IMPLS};
      assert not any("cuml" in n for n in et_names), message stating cuML must not appear in the
      ET group — it is RF (a different algorithm), in RF-vs-RF group only.
    - test_benchmark_impl_algorithm_tags_valid: for each impl in ET_IMPLS + RF_IMPLS:
      assert impl.algorithm in ("et", "rf").
  </action>
  <verify>
    <automated>cd python && python -m pytest tests/test_crossover_manifest.py tests/test_crossover_fairness_rules.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>crossover_manifest.py MANIFEST has all required keys including grid_n_values and grid_d_values matching the pre-registered lists. grid_spec.py exports SYNTHETIC_GRID (24 cells), make_grid_dataset returning float64 Dataset, BenchmarkImpl dataclass, ET_IMPLS and RF_IMPLS with sklearn factories. Both test files pass: manifest key-completeness + grid-match, fairness disjointness, n_jobs=-1 for all sklearn factories, no cuML in ET group.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-registration doc → measurement run | Protocol must precede measurement; git timestamp is the only enforcement — enforced by Task 1's commit requirement |
| grid_spec constants → fairness tests | SYNTHETIC_GRID must match the pre-registration document exactly; any divergence enables retroactive grid manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Repudiation | 07-PRE-REGISTRATION.md not committed before measurement | mitigate | Task 1 commits the file and the verify step checks git log; Plans 02 and 03 depend on this plan wave completing |
| T-07-02 | Tampering | SYNTHETIC_GRID in grid_spec.py diverges from pre-registration grid | mitigate | test_crossover_manifest.py asserts grid_n_values and grid_d_values match the pre-registered lists; a divergence causes CI failure |
| T-07-03 | Repudiation | ET-vs-RF comparison appears in the harness despite fairness rules | mitigate | test_crossover_fairness_rules.py asserts ET_IMPLS and RF_IMPLS are disjoint by name; cuML in ET group causes test failure |
| T-07-04 | Repudiation | sklearn baseline uses n_jobs other than -1 | mitigate | test_crossover_fairness_rules.py factory-instantiates every sklearn impl and asserts n_jobs == -1 |
| T-07-SC | Tampering | No new pip installs in this plan | accept | No package installs in Plan 01; all packages (sklearn, numpy) are already present in the parity venv |
</threat_model>

<verification>
- git log shows a commit for 07-PRE-REGISTRATION.md with message "pre-register" that PREDATES any crossover measurement commits.
- 07-PRE-REGISTRATION.md contains all 10 sections including the verbatim kill-criterion wording and an explicit prohibition on starting the timer after dtype coercion.
- pytest tests/test_crossover_manifest.py tests/test_crossover_fairness_rules.py passes (green) with no GPU required.
- crossover_manifest.py MANIFEST["grid_n_values"] == [10_000, 50_000, 100_000, 250_000, 500_000, 1_000_000].
- grid_spec.py make_grid_dataset returns Dataset with X_train.dtype == np.float64.
- ET_IMPLS and RF_IMPLS are disjoint sets by name.
</verification>

<success_criteria>
- The pre-registration document is committed to git before Plan 02 tasks begin (enforced by wave ordering).
- All fairness-rule unit tests pass CI-portably (no GPU required).
- The manifest and grid spec are structurally consistent with the pre-registration document.
- A developer reading 07-PRE-REGISTRATION.md knows exactly what grid will be run, what the kill criterion is, what the pivot path is, and what "win" means — with no ambiguity.
</success_criteria>

<output>
Create `.planning/phases/07-crossover-benchmark-gate-3/07-01-SUMMARY.md` when done.
</output>
