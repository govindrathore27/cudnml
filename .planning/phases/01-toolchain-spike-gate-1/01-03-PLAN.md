---
phase: 01-toolchain-spike-gate-1
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - crates/sylva-cuda/src/lib.rs
  - crates/sylva-cuda/benches/microbench.rs
  - crates/sylva-cuda/Cargo.toml
  - scripts/import_smoke.py
  - scripts/cupy_baseline.py
  - VERSIONS.md
autonomous: false
requirements:
  - TOOL-03
  - TOOL-04
user_setup:
  - service: cupy-baseline-venv
    why: "CuPy has NO cp314 Windows wheel (verified 01-RESEARCH.md); the microbench reference needs a separate Python 3.12 venv"
    env_vars: []
    dashboard_config:
      - task: "Create a Python 3.12 venv and install cupy-cuda12x for the microbench baseline (skip if using the raw-CUDA-C fallback)"
        location: "py -3.12 -m venv .venv-cupy ; .venv-cupy\\Scripts\\pip install cupy-cuda12x"

must_haves:
  truths:
    - "maturin build --release produces a cp310-abi3-win_amd64 wheel for sylva_cuda built with the dynamic-loading cudarc feature (D-02 shipping config)"
    - "In a fresh clean Python venv, pip install <wheel> then `import sylva_cuda` succeeds and a #[pyfunction] entrypoint calls into the Rust core (the run_vector_add path) — closing the 5-link skeleton chain (TOOL-03)"
    - "A fairness-encoded microbench reports cudarc+NVRTC per-launch overhead (us) and vector_add throughput (GB/s) vs the CuPy (or raw-CUDA-C) baseline, with the vector op verified correct (SC-6)"
    - "VERSIONS.md is finalized with all runtime fields filled and a definitive Kill-criteria result: proceed / WSL-fallback / stop, with one-line rationale (TOOL-04)"
  prohibitions:
    - "The microbench MUST NOT be reported as an algorithm speed claim — it is a feasibility sanity check only (PITFALLS Pitfalls 1/13); every reported number carries the no-algorithm-speed-claim label"
    - "MUST NOT report 'data already on GPU' timings as the headline — warmup + repeated runs + median, device-event timing, transfers acknowledged"
    - "No Extra Trees / Random Forest / SHAP / estimator logic added to the PyO3 entrypoint — it exposes only the throwaway spike kernels"
    - "The wheel build MUST use cudarc dynamic-loading (not the static cuda-12080 launch-proof feature)"
  artifacts:
    - path: "scripts/import_smoke.py"
      provides: "Clean-venv import + entrypoint-call smoke test for the abi3 wheel"
      contains: "import sylva_cuda"
    - path: "crates/sylva-cuda/benches/microbench.rs"
      provides: "Warmup + CUDA-event launch-overhead + GB/s harness (fairness-encoded, no algorithm speed claim)"
      contains: "warmup"
    - path: "VERSIONS.md"
      provides: "Finalized TOOL-04 artifact with the kill-decision verdict"
      contains: "proceed"
  key_links:
    - from: "crates/sylva-cuda/src/lib.rs"
      to: "crates/sylva-cuda/src/nvrtc_launch.rs"
      via: "#[pyfunction] entrypoint calls run_vector_add / run_histogram"
      pattern: "run_vector_add"
    - from: "scripts/import_smoke.py"
      to: "crates/sylva-cuda/src/lib.rs"
      via: "import sylva_cuda; sylva_cuda.<entrypoint>() exercises the Rust core in a clean venv"
      pattern: "sylva_cuda"
---

<objective>
Close the walking-skeleton chain and decide the gate. Build the PyO3 0.29 + maturin `abi3` wheel with cudarc `dynamic-loading` (D-02's real shipping config — CUDA resolved at runtime), prove it imports in a clean Python venv and calls into the Rust core (TOOL-03), run the fairness-encoded kernel-launch/vector-op microbench vs the CuPy (or raw-CUDA-C) baseline (SC-6), and FINALIZE `VERSIONS.md` with the definitive kill-decision (TOOL-04).

This is the last two links of the five-link skeleton: wheel BUILDS (maturin abi3) and wheel IMPORTS in a clean venv. After this plan, `import sylva_cuda; sylva_cuda.run_vector_add(...)` works end-to-end from a fresh Python environment, proving the entire Rust -> CUDA(NVRTC) -> Python(abi3) path natively on Windows/MSVC with no WSL.

Purpose: Validate packaging (the D-02 dynamic-loading shipping mode) now rather than deferring it, deliver the microbench feasibility sanity check (explicitly NO algorithm speed claim), and produce the durable proceed/WSL-fallback/stop verdict that is the phase's actual deliverable.
Output: A `cp310-abi3-win_amd64` wheel, a clean-venv import smoke pass, microbench numbers, and a finalized `VERSIONS.md` with the kill-decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-toolchain-spike-gate-1/01-CONTEXT.md
@.planning/phases/01-toolchain-spike-gate-1/01-RESEARCH.md
@.claude/CLAUDE.md
@.planning/phases/01-toolchain-spike-gate-1/01-01-SUMMARY.md
@.planning/phases/01-toolchain-spike-gate-1/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose a PyO3 entrypoint and build the abi3 wheel (dynamic-loading) (TOOL-03)</name>
  <read_first>
    - .planning/phases/01-toolchain-spike-gate-1/01-RESEARCH.md ("Standard Stack" pyproject.toml + maturin commands; Pitfall 1 PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1 on Py3.14; Open Question 1 + A2 fallback build against 3.10-3.13 interpreter; "Architecture Patterns" system diagram — dynamic-loading wheel resolves CUDA at runtime)
    - .planning/phases/01-toolchain-spike-gate-1/01-CONTEXT.md (D-02 wheel uses dynamic-loading; D-03 abi3-py310 covers Py3.14)
    - crates/sylva-cuda/src/lib.rs, crates/sylva-cuda/src/nvrtc_launch.rs (the #[pymodule] seam + run_vector_add/run_histogram from Plans 01/02)
    - .claude/CLAUDE.md (PyO3 abi3 one-wheel; no silent fallback — PyO3 maps cudarc errors to Python exceptions)
  </read_first>
  <files>crates/sylva-cuda/src/lib.rs, crates/sylva-cuda/Cargo.toml</files>
  <action>
    In `lib.rs`, add a thin `#[pyfunction]` `run_vector_add` (and optionally `run_histogram`) that accepts plain Python lists / numpy-free f32 sequences, validates length at the boundary, calls the Rust `run_vector_add` from `nvrtc_launch.rs`, and returns the result — mapping any cudarc/crate error to a `PyErr` (a clean Python exception, never a silent fallback). Register it in the `#[pymodule] sylva_cuda`. For the WHEEL build, switch the cudarc features to the dynamic-loading shipping set: build with `cudarc = { version = "0.19.8", default-features = false, features = ["driver", "nvrtc", "dynamic-loading"] }` — implement this as a Cargo feature toggle (e.g. a `wheel`/`dynamic` feature that selects dynamic-loading) OR document the manifest swap, so BOTH D-02 modes are provably exercised (static for the Plan 02 launch proof, dynamic for this wheel). Pre-arm `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1` in the build environment (Pitfall 1); if the build still fails on the Py3.14 interpreter, fall back to building against an installed 3.10-3.13 interpreter via `maturin build -i <python3.12>` (the abi3 wheel still runs on 3.14 — Task 2 verifies that). Run `maturin build --release` and confirm a `*cp310-abi3-win_amd64*.whl` is produced under `target/wheels/`. Record in VERSIONS.md (Task 3) whether the env var / interpreter-fallback was needed.
  </action>
  <verify>
    <automated>cargo build -p sylva-cuda --release</automated>
  </verify>
  <acceptance_criteria>
    - `maturin build --release` produces a wheel whose filename contains `cp310-abi3` and `win_amd64`
    - The wheel build uses cudarc `dynamic-loading` (not `cuda-12080`) — verifiable in the build invocation / feature-gated manifest
    - `lib.rs` registers a `#[pyfunction]` that calls `run_vector_add` and maps errors to `PyErr` (no `.unwrap()` across the FFI boundary)
    - VERSIONS.md notes whether `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1` or an interpreter fallback was required
  </acceptance_criteria>
  <done>An abi3 wheel built with the dynamic-loading shipping config exposes a PyO3 entrypoint into the Rust CUDA core; both D-02 linking modes (static launch-proof + dynamic wheel) are now exercised.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install the wheel in a clean venv and prove `import sylva_cuda` + call works (TOOL-03)</name>
  <read_first>
    - .planning/phases/01-toolchain-spike-gate-1/01-RESEARCH.md (Validation Architecture: clean-venv import smoke; Phase Requirements -> Test Map TOOL-03 command `pip install <wheel> && python -c "import sylva_cuda"`)
    - .planning/phases/01-toolchain-spike-gate-1/01-CONTEXT.md (TOOL-03 minimal wheel imports in clean env)
    - crates/sylva-cuda/src/lib.rs (the #[pyfunction] entrypoint from Task 1)
  </read_first>
  <files>scripts/import_smoke.py</files>
  <action>
    Create a brand-new empty Python 3.14 venv (the project interpreter), install ONLY the built wheel into it (no repo source on PYTHONPATH), and run `scripts/import_smoke.py` which does `import sylva_cuda`, prints `sylva_cuda.__file__`, and calls the entrypoint (`run_vector_add` on a tiny vector) asserting the result is correct — proving the dynamic-loading wheel resolves the CUDA driver at runtime and the full Rust->CUDA->Python path works from a clean install. See `<how-to-verify>` for exact commands and expected output.
  </action>
  <what-built>
    The agent creates a brand-new empty Python venv (Python 3.14.3, the project interpreter), installs ONLY the built wheel into it (no source tree on PATH), and runs `scripts/import_smoke.py` which does `import sylva_cuda`, prints `sylva_cuda.__file__`, and calls the entrypoint (`run_vector_add` on a tiny vector) asserting the result is correct. This proves the dynamic-loading wheel resolves the CUDA driver at runtime and the full Rust->CUDA->Python path works from a clean install — the final link of the skeleton chain.
  </what-built>
  <how-to-verify>
    1. `py -3.14 -m venv .venv-smoke` (a fresh venv, NOT the dev environment).
    2. `.venv-smoke\Scripts\pip install target\wheels\<the cp310-abi3-win_amd64 wheel>`.
    3. `.venv-smoke\Scripts\python scripts\import_smoke.py` -> expect it to print a `sylva_cuda` path under `.venv-smoke\Lib\site-packages` and print `OK: run_vector_add correct`.
    4. Confirm exit code 0 and that the printed module path is inside the venv (not the repo source).
  </how-to-verify>
  <acceptance_criteria>
    - `.venv-smoke\Scripts\python -c "import sylva_cuda; print(sylva_cuda.__file__)"` exits 0 and prints a path inside `.venv-smoke`
    - `scripts/import_smoke.py` calls the entrypoint and asserts the vector_add result is correct, exiting 0
    - The import succeeds with ONLY the wheel installed (no repo source on PYTHONPATH)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the wheel imports and the entrypoint call succeeds in the clean venv, or paste the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the fairness-encoded microbench vs the CuPy/raw baseline and finalize VERSIONS.md (SC-6, TOOL-04)</name>
  <read_first>
    - .planning/phases/01-toolchain-spike-gate-1/01-RESEARCH.md ("Code Examples: Microbench timing" fairness rules — warmup ~50, median of >=10 runs, CUDA-event timing, GB/s = 3*4*N/time, assert correctness first; Pitfall 2 CuPy cp314 gap -> Py3.12 venv or raw-CUDA-C; "WSL-Fallback Decision Tree" VERSIONS.md required fields + verdict)
    - .planning/research/PITFALLS.md (Pitfalls 1, 2, 13 — comparative-study fairness; no "data already on GPU" headline; no algorithm speed claim at this phase)
    - .planning/ROADMAP.md (Phase 1 Comparative Baseline Study: metric, pass bar <= ~2-3x baseline per-launch overhead + correct vector op; "feasibility sanity check, not a speed gate"; the KILL CRITERION)
    - .planning/phases/01-toolchain-spike-gate-1/01-CONTEXT.md (D-06 CuPy preferred, Py3.12 venv fallback; D-05 kill-criteria proceed/WSL/stop)
    - VERSIONS.md (the Plan 01 template to finalize)
  </read_first>
  <files>crates/sylva-cuda/benches/microbench.rs, scripts/cupy_baseline.py, VERSIONS.md</files>
  <action>
    Implement `microbench.rs` per the fairness-encoded recipe: (1) WARMUP ~50 discarded launches; (2) launch-overhead = tight loop of N near-empty launches / N in us/launch via CUDA events; (3) throughput = vector_add on the FIXED 1e7-element f32 vector, bytes = 3*4*N (two reads + one write), GB/s = bytes / median_time, reporting the MEDIAN of >=10 timed runs (not mean); (4) assert max_abs_err(out, a+b) == 0 BEFORE reporting any timing. Create `scripts/cupy_baseline.py` running the equivalent CuPy vector-add launch-overhead + GB/s measurement (same N, same warmup/median protocol) in the separate Python 3.12 venv (D-06 / Pitfall 2). If CuPy is unavailable, add a raw-CUDA-C in-process baseline kernel via the same NVRTC path as the documented fallback (Option a) and note which baseline was used. Compute the ratio (cudarc per-launch overhead / baseline per-launch overhead). Then FINALIZE `VERSIONS.md`: fill every runtime field (exact Rust version from Plan 01, the PYO3_USE_ABI3_FORWARD_COMPATIBILITY outcome from Plan 03 Task 1, the CuPy/raw baseline env, the four compute-sanitizer "0 errors" outcomes from Plan 02, the microbench us/launch + GB/s for both paths LABELED "feasibility sanity check, no algorithm speed claim"), and write the definitive `Kill-criteria result:` verdict by walking the WSL-Fallback Decision Tree — proceed if TOOL-01..03 green + microbench within ~2-3x baseline; WSL-fallback if only the wheel/link step failed natively; stop if no path sanitizes/launches anywhere — with a one-line rationale. Reference the committed Cargo.lock.
  </action>
  <verify>
    <automated>cargo bench -p sylva-cuda --bench microbench -- --test && grep -q "Kill-criteria result" VERSIONS.md && grep -q "no algorithm speed claim" VERSIONS.md && grep -Eq "proceed|WSL-fallback|stop" VERSIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo bench -p sylva-cuda --bench microbench` runs, asserts vector-op correctness before timing, and reports us/launch + GB/s
    - `crates/sylva-cuda/benches/microbench.rs` contains warmup logic and uses median (not mean) of >=10 runs
    - `VERSIONS.md` contains a definitive `Kill-criteria result:` of `proceed`, `WSL-fallback`, or `stop` with a one-line rationale
    - `VERSIONS.md` labels the microbench numbers `no algorithm speed claim` and records both cudarc and baseline us/launch + GB/s
    - The cudarc-vs-baseline per-launch overhead ratio is recorded against the <= ~2-3x pass bar
  </acceptance_criteria>
  <done>The microbench reports launch overhead + throughput for cudarc+NVRTC vs the CuPy/raw baseline (correctness asserted first, fairness rules encoded, explicitly no algorithm speed claim), and VERSIONS.md is finalized with the kill-decision verdict — TOOL-04 complete, the phase gate decided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wheel install -> clean venv | The built wheel is pip-installed into a fresh venv; dynamic-loading resolves the CUDA driver at runtime |
| Python -> Rust (PyO3 FFI) | Python sequence length crosses into the Rust core; validated before the device launch |
| CuPy baseline venv -> same GPU | A separate Py3.12 process measures the baseline against the same RTX 4060 Ti (fair comparison) |

Local spike: no network services, no auth, no persisted secrets, no PII. Surface is supply-chain (the cupy-cuda12x baseline package) and FFI input validation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering / DoS | unvalidated Python sequence length at the PyO3 boundary | mitigate | Validate length at the #[pyfunction] boundary before launch; map errors to PyErr (no silent fallback) |
| T-01-07 | Spoofing | cupy-cuda12x baseline install in the Py3.12 venv | accept | cupy-cuda12x is the official CuPy CUDA-12 wheel, cleared as a false-positive SUS in 01-RESEARCH.md "Package Legitimacy Audit"; baseline-only, isolated venv, throwaway — low value |
| T-01-SC | Tampering | pip installs (wheel, cupy) in this plan | mitigate | No `[ASSUMED]`/`[SUS]` packages requiring a human-verify checkpoint remain — cupy-cuda12x's SUS verdict was independently cleared as a false positive (official CuPy wheel) in 01-RESEARCH.md. Pin the baseline version recorded in VERSIONS.md. |

No high-severity threats: the wheel and baseline run locally with fixed test inputs; no network listener, no untrusted external data, no secrets. PyO3 maps cudarc Results to Python exceptions, preserving the no-silent-fallback contract across the FFI boundary.
</threat_model>

<verification>
- `maturin build --release` produces a `cp310-abi3-win_amd64` wheel built with cudarc dynamic-loading (D-02 shipping config).
- A clean venv imports `sylva_cuda` and the entrypoint call into the Rust CUDA core succeeds (TOOL-03) — the 5-link skeleton chain is closed.
- The microbench reports us/launch + GB/s vs the CuPy/raw baseline, correctness asserted first, fairness rules encoded, explicitly no algorithm speed claim (SC-6).
- `VERSIONS.md` is finalized with all pins + the definitive proceed/WSL-fallback/stop verdict (TOOL-04).
</verification>

<success_criteria>
- TOOL-03: minimal PyO3 + maturin abi3 wheel builds and imports in a clean Windows Python env, calling into the Rust core.
- TOOL-04: pinned versions recorded + documented kill-criteria result (proceed / WSL-fallback / stop).
- SC-6: kernel-launch/vector-op microbench within the pass bar, with no algorithm speed claim.
- Both D-02 linking modes (static launch-proof + dynamic wheel) exercised; the walking-skeleton chain is complete end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-toolchain-spike-gate-1/01-03-SUMMARY.md` when done.
</output>
