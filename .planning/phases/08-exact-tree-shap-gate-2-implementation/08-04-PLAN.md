---
phase: 08-exact-tree-shap-gate-2-implementation
plan: 04
type: execute
wave: 3
depends_on:
  - "08-03"
files_modified:
  - crates/sylva-shap/src/gpu.rs
  - crates/sylva-shap/Cargo.toml
  - scripts/benchmarks/shap_speedup.py
autonomous: false
requirements:
  - SHAP-02
must_haves:
  truths:
    - "Gate-2 GPU path verdict (Option A or C) from 08-01-GATE2-DECISION.md is honored"
    - "If Option A: GPU SHAP kernel in CUDA C (not C++) compiled via NVRTC; no C++ headers included"
    - "If Option C: gpu.rs is a stub returning ShapError::CudaError with a clear 'GPU SHAP deferred' message"
    - "Comparative Baseline Study report committed: sylva-shap GPU vs shap.TreeExplainer CPU timing, with agreement reported alongside speed"
    - "Speedup is reported (not gated); agreement within atol=1e-4 is still required alongside speed"
    - "Apache-2.0 reimplementation: GPU kernel derived from GPUTreeSHAP algorithm description; no C++ source copied"
  artifacts:
    - path: "crates/sylva-shap/src/gpu.rs"
      provides: "compute_shap_gpu stub (Option C) OR CUDA-C NVRTC kernel dispatch (Option A)"
    - path: "scripts/benchmarks/shap_speedup.py"
      provides: "SHAP-compute speedup study: sylva GPU vs shap.TreeExplainer CPU vs GPUTreeSHAP, with agreement beside speed"
  key_links:
    - from: "scripts/benchmarks/shap_speedup.py"
      to: "crates/sylva-core/src/pyseam.rs"
      via: "sylva.shap_values(ir_handle, X) called on GPU device for timing"
      pattern: "shap_values"
user_setup:
  - service: "GPUTreeSHAP (optional speed baseline)"
    why: "Speed comparison baseline in the comparative study (reported only, not a gate)"
    env_vars: []
    dashboard_config:
      - task: "GPUTreeSHAP is not pip-installable (no maintained release since 2022); skip if unavailable — label as 'GPUTreeSHAP baseline: unavailable' in the report"
        location: "n/a"
---

## Phase Goal

**As a** developer finalizing the Phase 8 comparative baseline study, **I want to** execute the GPU SHAP path (if spike approved it) and report timing vs shap.TreeExplainer CPU with agreement alongside, **so that** SHAP-02's GPU path is delivered or honestly deferred and the comparative study is committed.

<execution_context>
@C:\Users\PC\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\PC\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-exact-tree-shap-gate-2-implementation/08-RESEARCH.md
@.planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md
@crates/sylva-cuda/Cargo.toml
@crates/sylva-shap/Cargo.toml
@crates/sylva-core/src/pyseam.rs
</context>

## Assumptions (per orchestrator — no CONTEXT.md)

- **D-07 (GPU path contingent on Gate-2 verdict):** If 08-01-GATE2-DECISION.md records Option A, implement the GPU kernel. If Option C, gpu.rs is a descriptive stub and the speedup study omits the GPU column. Alternative: force GPU regardless — rejected (the gate-2 checkpoint exists precisely to avoid this). Record the chosen path in the Assumptions block of this plan's SUMMARY.
- **D-08 (CUDA C only, no C++ in NVRTC):** GPU kernel must be authored in CUDA C, not C++. GPUTreeSHAP's C++ headers are explicitly excluded. Reimplementation from the Apache-2.0 algorithm description only. Alternative: bind to GPUTreeSHAP C++ header — rejected (NVRTC cannot compile C++, confirmed in Gate-2 spike).
- **D-09 (speedup is reported, not gated):** Agreement within atol=1e-4 is still required alongside speed. A faster-but-wrong result is not a win. Fairness rules: end-to-end including data prep, cold + warm, pinned versions, agreement column always present.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Read Gate-2 verdict and choose implementation path</name>

  <what-built>
    Read .planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md and extract the GPU path verdict. Present the verdict to the developer and confirm which task body to execute: Task 1A (GPU kernel) or Task 1C (CPU-only stub).
  </what-built>

  <how-to-verify>
    Read 08-01-GATE2-DECISION.md. Confirm the GPU path field is present (Option A or Option C). No implementation starts until the developer confirms which path to take.
  </how-to-verify>

  <resume-signal>Type "option-a" to proceed with the CUDA C GPU kernel, or "option-c" to proceed with the CPU-only stub and speedup study. If option-a, also note the maximum GPU depth tested in the spike.</resume-signal>

  <files>.planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md</files>
  <action>Read 08-01-GATE2-DECISION.md. Extract the GPU path verdict field (Option A or Option C). Present verdict and await developer confirmation before proceeding to Task 1.</action>
  <verify>
    <automated>grep -i "option" .planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md | head -3</automated>
  </verify>
  <done>Developer has confirmed "option-a" or "option-c" in response; Task 1 proceeds accordingly.</done>
</task>

<task type="auto">
  <name>Task 1: gpu.rs — GPU SHAP kernel (Option A) or descriptive stub (Option C)</name>
  <files>crates/sylva-shap/src/gpu.rs, crates/sylva-shap/Cargo.toml</files>
  <read_first>
    - crates/sylva-cuda/Cargo.toml lines 73-104 (cudarc feature flags pattern; cuda-static vs wheel toggle)
    - .planning/phases/08-exact-tree-shap-gate-2-implementation/08-RESEARCH.md lines 63-80 (GPU integration path options A and C; pitfall on C++ incompatibility with NVRTC)
    - .planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md (the actual verdict to honor)
  </read_first>
  <action>
    Honor the verdict from Task 0 / 08-01-GATE2-DECISION.md:

    OPTION C (CPU-only stub — most likely outcome given NVRTC/C++ constraints):
    Create crates/sylva-shap/src/gpu.rs with a single pub function:
    pub fn compute_shap_gpu(_ir: &ForestIR, _x: ArrayView2<f32>) -> Result<ShapOutput, ShapError>
    returning Err(ShapError::CudaError("GPU SHAP deferred to v2: use compute_shap_cpu for Phase 8".into())).
    Add a module doc comment: "GPU SHAP kernel — Phase 8 stub. GPU path deferred to v2 per Gate-2 verdict (Option C). See 08-01-GATE2-DECISION.md for rationale."
    No cudarc dependency needed for this option.
    In crates/sylva-shap/Cargo.toml, add a [features] section: cuda = [] (placeholder for Wave 3 v2 work).

    OPTION A (CUDA C GPU kernel — proceed only if Gate-2 decision document explicitly records Option A):
    Create crates/sylva-shap/src/gpu.rs implementing the PathElement-extraction + parallel Shapley kernel following the GPUTreeSHAP Apache-2.0 algorithm description (NOT copying C++ source):
    1. PathElement extraction: for each tree, depth-first enumerate all root-to-leaf paths, producing a flat Vec<PathElement> with fields {feature_idx: i32, zero_fraction: f32, one_fraction: f32, path_len: u32, leaf_value: f32}.
    2. CUDA C kernel (inline CUDA C string compiled via cudarc NVRTC at runtime): each CUDA thread handles one (row, path) pair; accumulates phi contributions per the EXTEND/UNWIND formula in shared memory; atomicAdd to global phi output buffer. Kernel written in CUDA C dialect — no C++ templates, no STL, no thrust.
    3. Use cudarc 0.19.8 for kernel launch (same pattern as sylva-cuda Phase 1). Add cudarc as optional dep in sylva-shap Cargo.toml: cudarc = { version = "0.19.8", optional = true, default-features = false, features = ["std", "driver", "nvrtc"] }. Gate under [features] cuda = ["dep:cudarc", "sylva-core/cuda-static"] (or "wheel" for distribution).
    4. Add compute-sanitizer clean target in comments: SAFETY note that the kernel must be validated with compute-sanitizer memcheck + racecheck before ship.
    5. Agreement gate: after GPU kernel returns, verify atol=1e-4 vs CPU result by running compute_shap_cpu on same input in a debug assert (debug_assert! in test, not in production).

    Regardless of option, update crates/sylva-shap/src/lib.rs to include:
    #[cfg(feature = "cuda")] pub mod gpu;
    and update the pub use block if Option A is taken.
  </action>
  <verify>
    <automated>cargo build -p sylva-shap 2>&1 | tail -5</automated>
  </verify>
  <done>cargo build -p sylva-shap exits 0. If Option C: gpu.rs returns descriptive error, no cudarc dependency active. If Option A: cargo build -p sylva-shap --features cuda exits 0 and the kernel compiles via NVRTC.</done>
</task>

<task type="auto">
  <name>Task 2: Comparative Baseline Study — shap_speedup.py (SHAP-03 comparative; reported, not gated)</name>
  <files>scripts/benchmarks/shap_speedup.py</files>
  <read_first>
    - .planning/phases/08-exact-tree-shap-gate-2-implementation/08-RESEARCH.md lines 488-503 (Comparative Baseline Study metrics and baselines)
    - .planning/ROADMAP.md Phase 8 Comparative Baseline Study section (fairness rules: agreement beside speed, cold+warm, pinned versions)
  </read_first>
  <action>
    Create scripts/benchmarks/shap_speedup.py implementing the Phase 8 Comparative Baseline Study per ROADMAP.md fairness rules:

    Structure:
    1. VERSION MANIFEST at top: print Python version, numpy version, shap version, torch/cupy version if available, CUDA driver version from cudarc (if GPU path active), sylva_core_pyseam module path.
    2. DATASET: sklearn make_classification(n_samples=10000, n_features=54, random_state=42) (Covertype-like dimensions at 10k rows for reasonable shap.TreeExplainer timing). Train a forest with n_estimators=100, max_depth=12, seed=42, algo="et" via sylva.fit_cpu. Use the same ir_json for all baselines.
    3. AGREEMENT CHECK (must pass before timing): compute sylva shap_values on 100 rows; compute shap.TreeExplainer shap_values on same rows; assert max_abs_error < 1e-4 for shallow (max_depth=5 forest) and < 5e-4 for deep (max_depth=12). Print: "Agreement check: PASS (max_abs={:.2e})". If it fails: print "Agreement check: FAIL — do not report speedup" and exit 1.
    4. TIMING LOOP: measure wall-clock time with time.perf_counter for each baseline on 1000 explain rows, 3 cold runs (no warm-up) and 5 warm runs:
       - sylva CPU: sylva.shap_values(ir_json, X_explain)
       - shap.TreeExplainer CPU: explainer.shap_values(X_explain) (custom-dict path, same model)
       - sylva GPU (if Option A active and CUDA available): sylva.shap_values_gpu(ir_json, X_explain) — else label "GPU: deferred (Option C)"
       - GPUTreeSHAP reference: label "GPUTreeSHAP: unavailable (no maintained pip release since 2022)" — do not attempt install
    5. REPORT: print a markdown table:
       | Method | Cold 1 (s) | Warm avg (s) | Rows/s | Agreement (max_abs_err) |
       Explicitly state: "Speed numbers are informational. Agreement within atol is the gate (SHAP-03). A faster-but-wrong result is not a win."
    6. Honest OOM/failure reporting: wrap each timing in try/except; if any baseline raises OOM or RuntimeError, record "OOM/FAIL" in the table cell and continue.

    Add fairness-rule header at the top of the script as a multi-line comment (not just docstring):
    "FAIRNESS RULES: attributions vs attributions (exact, not interventional); end-to-end from numpy including data prep; cold vs warm separated; agreement reported alongside speed; versions pinned; OOM/failures reported honestly."
  </action>
  <verify>
    <automated>cd python && python ../scripts/benchmarks/shap_speedup.py 2>&1 | grep -c "Agreement check: PASS"</automated>
  </verify>
  <done>
    scripts/benchmarks/shap_speedup.py exits 0 with "Agreement check: PASS" printed; a markdown timing table is printed with at least two rows (sylva CPU and shap.TreeExplainer CPU); agreement column is present in every row; fairness disclaimer is visible in output.
    The speedup study artifact (stdout output) is committed as COMPARATIVE-STUDY.md in the phase directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark script → GPU | Untrusted array sizes passed to GPU kernel; NVRTC compile at runtime |
| benchmark script → shap.TreeExplainer | Third-party library call; only legitimate MIT-licensed shap used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | GPU SHAP kernel — out-of-bounds node index in PathElement | mitigate | If Option A: add bounds check before PathElement construction; GPU kernel reads only pre-validated path data; IR validated via validate_structure() before path extraction |
| T-08-04-02 | Tampering | GPU SHAP kernel — CUDA C only, no C++ headers | mitigate | NVRTC source string must not include angle-bracket headers from the C++ standard library or CUDA C++ wrappers; include only standard C headers (stdint.h, math.h) |
| T-08-04-03 | Information Disclosure | benchmark script reports wrong agreement as PASS | mitigate | Agreement check uses atol=1e-4 (shallow) / 5e-4 (deep) and exits 1 on failure — prevents false reporting |
| T-08-04-04 | DoS | benchmark script hangs on GPU OOM | mitigate | Wrap each GPU call in try/except; report OOM/FAIL in table and continue |
| T-08-04-SC | Tampering | shap pip install | accept | Gate-2 (Plan 01) confirmed shap 0.52.0 is legitimate MIT library; installed in test venv already in Wave 2 |
</threat_model>

<verification>
Full verification sequence:
1. cargo build -p sylva-shap exits 0 (Option C stub or Option A CUDA kernel)
2. cargo test --workspace exits 0 (no regressions)
3. python scripts/benchmarks/shap_speedup.py exits 0 with "Agreement check: PASS" and a printed timing table
4. If Option A: compute-sanitizer memcheck against the GPU SHAP kernel passes (no errors)
5. COMPARATIVE-STUDY.md committed to .planning/phases/08-exact-tree-shap-gate-2-implementation/
</verification>

<success_criteria>
- gpu.rs exists (stub or real kernel per Gate-2 verdict)
- scripts/benchmarks/shap_speedup.py: agreement check passes before any speed number is printed
- Timing table printed with at minimum sylva-CPU and shap.TreeExplainer-CPU rows
- Agreement column present in every timing row (faster-but-wrong is explicitly penalized)
- COMPARATIVE-STUDY.md committed with the printed output as a permanent artifact
- SHAP-02 GPU requirement honored as committed (Option A implemented) or honestly deferred (Option C documented)
</success_criteria>

<output>
Create `.planning/phases/08-exact-tree-shap-gate-2-implementation/08-04-SUMMARY.md` when done.
Also create `.planning/phases/08-exact-tree-shap-gate-2-implementation/COMPARATIVE-STUDY.md` from the benchmark stdout.
</output>
