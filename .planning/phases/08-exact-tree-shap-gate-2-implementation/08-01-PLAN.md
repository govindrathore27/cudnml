---
phase: 08-exact-tree-shap-gate-2-implementation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md
autonomous: false
requirements:
  - SHAP-01
must_haves:
  truths:
    - "Gate-2 feasibility decision record exists before any implementation begins"
    - "WoodelfHD LICENSE verified by direct reading (not websearch assumption)"
    - "GPUTreeSHAP Apache-2.0 license confirmed by direct reading"
    - "Kill criterion evaluated: neither prong fires (interactions deferred, WoodelfHD is MIT)"
    - "GPU integration path for SHAP decided: Option A (CUDA C reimplementation) or Option C (CPU-only)"
    - "Scope locked: exact attributions only; SHAP-V2-01 interactions are v2, deferred"
  artifacts:
    - path: ".planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md"
      provides: "Committed feasibility decision record with license findings + GPU path verdict"
  key_links: []
---

## Phase Goal

**As a** developer building Sylva, **I want to** confirm the SHAP feasibility gate (scope, licenses, GPU path) before writing any implementation code, **so that** implementation waves are blocked on a committed decision record and we never build in a direction the kill criterion would reject.

<execution_context>
@C:\Users\PC\.claude\gsd-core\workflows\execute-plan.md
@C:\Users\PC\.claude\gsd-core\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-exact-tree-shap-gate-2-implementation/08-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Gate-2 Feasibility Spike — License Verification + GPU Path Decision</name>

  <what-built>
    Research and a committed decision document covering:
    1. WoodelfHD (github.com/ron-wettenstein/woodelf) LICENSE file — read directly, not from websearch
    2. GPUTreeSHAP (github.com/rapidsai/gputreeshap) LICENSE file — confirm Apache-2.0
    3. shap Python package (pypi.org/project/shap) — confirm MIT license
    4. Kill criterion evaluation: (a) WoodelfHD GPL/closed? and (b) interactions the target?
    5. GPU integration path decision: Option A (CUDA C reimplementation following Apache-2.0 algorithm) vs Option C (CPU-only Phase 8, GPU deferred)
    6. Scope confirmation: exact attributions only; SHAP-V2-01 interactions are v2

    Claude must: (a) fetch each LICENSE file via WebFetch to confirm license text; (b) record findings in 08-01-GATE2-DECISION.md; (c) present a clear proceed/descope verdict.

    The following URLs to check:
    - https://raw.githubusercontent.com/ron-wettenstein/woodelf/main/LICENSE
    - https://raw.githubusercontent.com/rapidsai/gputreeshap/main/LICENSE
    - https://pypi.org/project/shap/

    For the GPU path decision, spend no more than 2 hours sketching whether the core GPUTreeSHAP algorithm (path-extraction + parallel Shapley summation) is expressible in CUDA C without C++ templates. Key question: the PathElement struct and EXTEND/UNWIND DP from the algorithm description — do they require STL/templates? Record the assessment even if the answer is "needs further investigation": the acceptable outcomes are (A) "CUDA C is sufficient — proceed with Wave 3 GPU path" or (C) "GPU path blocked in Phase 8 — CPU-only is complete deliverable, GPU deferred to v2". Option C is not a failure; CPU-only Phase 8 is the stated primary deliverable.
  </what-built>

  <how-to-verify>
    1. Read .planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md
    2. Confirm it contains:
       - WoodelfHD LICENSE text or quoted excerpt (confirm MIT, not GPL)
       - GPUTreeSHAP LICENSE text (confirm Apache-2.0)
       - shap PyPI license (confirm MIT)
       - Kill criterion evaluation: "WoodelfHD GPL? NO. Interactions the target? NO. Kill criterion fires? NO."
       - GPU path verdict: Option A or Option C, with rationale
       - Scope lock statement: "Phase 8 implements SHAP attributions only; SHAP-V2-01 interactions are v2 and deferred"
    3. Verify none of the kill criterion prongs fired
    4. Confirm the document is committed to git before Wave 1 begins
  </how-to-verify>

  <resume-signal>Type "gate2-approved" after reviewing the decision document, or describe any issues found (e.g., unexpected GPL license, kill criterion fires).</resume-signal>

  <files>.planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md</files>
  <action>
    Fetch each LICENSE URL via WebFetch and record verbatim license type. Evaluate kill criterion prongs. Write 08-01-GATE2-DECISION.md with all six required sections. Commit the file.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-GATE2-DECISION.md && echo "exists" || echo "MISSING"</automated>
  </verify>
  <done>08-01-GATE2-DECISION.md committed; developer has typed "gate2-approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external URL → decision document | WebFetch reads live license files; content must be verified for legitimacy (not spoofed/redirected) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | WebFetch of GitHub LICENSE files | mitigate | Read from raw.githubusercontent.com canonical URLs; verify content begins with recognizable license text (MIT/Apache-2.0 header) |
| T-08-01-02 | Information Disclosure | Decision record in .planning | accept | Document lives in .planning/ (not committed to dist); no PII or secrets |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | Wave 0 installs nothing; Gate-2 is read-only research |
</threat_model>

<verification>
Gate-2 is complete when 08-01-GATE2-DECISION.md exists, is committed to git, and contains all six required sections (three license confirmations, kill criterion evaluation, GPU path verdict, scope lock). Implementation waves MUST NOT start before this checkpoint is approved.
</verification>

<success_criteria>
- 08-01-GATE2-DECISION.md committed to git with: WoodelfHD MIT confirmed, GPUTreeSHAP Apache-2.0 confirmed, shap MIT confirmed, kill criterion evaluated and NOT fired, GPU path decided (A or C), scope = attributions only
- Human reviewer has typed "gate2-approved"
- SHAP-01 requirement is satisfied by this committed decision record
</success_criteria>

<output>
Create `.planning/phases/08-exact-tree-shap-gate-2-implementation/08-01-SUMMARY.md` when done
</output>
